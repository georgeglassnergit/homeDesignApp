# DEV-LOG

*Append-only, newest entry on top. The autonomous daily run writes here each morning.*

---

## 2026-08-10 — Phase 3 · D1: import a plan (trace an uploaded floor plan underlay)
- **Picked:** **D1**, the recommended-next slice in `PHASE-3-PLAN.md` (§ Recommended sequence #3, after Group B + E1) — and the **consensus next** flagged by every open PR's "Next" note. *Pick protocol honoured:* `git fetch origin main` → branched clean off `origin/main` tip `6ab7294` (= merged PR #23). `list_pull_requests(state=open)` returned **three stacked PRs** — **#24** (B2 roof overhang; owns `core/roofShape.js`, `build/geometry.js`, roof region of `main.js`), **#25** (E1 materials; owns `core/model.js`, `edit/commands.js`, `app/inspector.js`), **#26** (E3 export; owns new `core/exportObj.js`, `index.html` export region). So B2/E1/E3 are all taken. **D1 is the lowest-collision remaining slice:** its substance is a **brand-new pure module** + `app/planCanvas.js` (owned by no open PR), and it treats the underlay as **display state that never touches the geometry save**, so it avoids `model.js`/`commands.js`/`inspector.js`/`roofShape.js`/`geometry.js` entirely. Corrected stale ledger rows (B1 → Landed PR #23; B2/E1/E3 → PR-open with their numbers).
- **The underlay is DISPLAY state — zero save-format risk.** A user imports a raster floor plan and traces walls over it. The image is a **session/display preference**, never serialized into the Phase 1 model — so old saves stay byte-identical and `serialize→deserialize→serialize` is unchanged (pure test 9a–9c prove importing/calibrating/moving an underlay leaves the project save byte-identical and leaks no field into the model or `meta`).
- **Model/view separation intact — zero Three.js added.** All the math is pure `core/underlay.js` (image↔world mapping + scale calibration); the plan is Canvas-2D. The separation guard (edit-core 30a–i) stays green, no allowlist change.
- **Changed:**
  - `src/core/underlay.js` — **new** pure module: `defaultMetersPerPixel` (a fresh image lands ~10 m wide), `createUnderlay`/`underlayWorldRect` (image as an axis-aligned world rectangle), `worldToImage`/`imageToWorld` (round-trippable pixel↔world), `calibrateScale` (multiply by known/measured), `rescaleUnderlay` (rescale about an **anchor** so traced content doesn't jump), `calibrateUnderlay` (the end-to-end "I drew a known dimension" flow), `withOpacity`/`moveUnderlay`.
  - `src/app/planCanvas.js` — draws the underlay beneath the grid/walls at its opacity (projected through the same `planView` the walls use, so it pans/zooms locked to the model); captures the two-click scale-calibration gesture (view state, no command); `setUnderlay`/`clearUnderlay`/`getUnderlay`/`hasUnderlay` + `startCalibrate`/`calibratePoint`/`cancelCalibrate`/`isCalibrating` hooks.
  - `src/app/state.js` — two new `FEATURE_TIERS` rows: `plan-import` (Simple — un-tiered import) + `plan-calibrate` (Pro — calibration).
  - `src/main.js` — the **now-live** "Import a plan" picker tile (loads a floor-plan image), a hidden file input, a "Plan image…" toolbar group + popover (opacity slider, Pro-gated **Calibrate scale**, Remove), the calibration flow (two clicks → prompt for the real length in the active units → anchored rescale), Esc cancels calibration, and headless handles (`__importUnderlay`, `__underlay`, `__setUnderlayOpacity`, `__calibrateUnderlay`, `__removeUnderlay`, seam-visibility getters).
  - `index.html` — the "Plan image…" group/popover markup + CSS + the hidden `<input type=file>`; the import tile is no longer a "coming soon" card (Outsource still is).
  - `src/test/underlay.test.mjs` — **new** 38-check pure suite.
  - `automation/headless-verify/probes/d1-import-plan.mjs` — **new** probe (synthesises a floor-plan image on an offscreen canvas, imports it through the real code path).
  - `docs/PHASE-3-PLAN.md` — ledger + seam table.
- **Verified — pure:** `node src/test/underlay.test.mjs` → **38/38** (default scale; create/validate; world rect; world↔image round-trip; `calibrateScale` known/measured + throws; anchored rescale keeps the anchor pixel fixed; the end-to-end calibrate flow reads the known length afterward; opacity/move copies; **the underlay never touches the geometry save**). Regression all green — edit-core **337** (separation guard green), gable-infill **25**, roof-ridge **40**, element-label **41**, room-hover **15**, room-rename **18**, `phase2/test/phase2-core` **33**. **547 assertions, 0 fail.**
- **Verified — headless** (vendored three r169 / csg v0.0.17 / meshbvh v0.7.6, real `src/main.js` over CDP under swiftshader): `verify.mjs phase3-import-plan --probe probes/d1-import-plan.mjs` → **21/21, zero console errors**. A synthesised 1000×800 plan imports at the default ~10 m width, the "Plan image…" group appears, opacity is a live tweak, **calibrate is hidden in Simple / shown in Pro**, a two-click calibration over a feature reading 5 m rescales the plan so that same feature now reads the entered 2 m (scale × 2/5, anchor pixel fixed), calibration issues **no command** and leaves the selection + save untouched, and remove clears/hides the underlay. Phase-1 5 walls / 4 CSG openings / 2 rooms / GLB furniture / lossless intact. Screenshot: `docs/verification/phase3-import-plan.png`.
- **Not touched (no regression):** the Phase 1 model + lossless save (the underlay is display-only, never serialized); phase2 core; CSG openings; GLB furniture; the roof/measure/room/rename/hover/label work; model/view separation (all new logic pure `core`/`app`, engine-free by the guard). No collision with the open B2/E1/E3 PRs.
- **Next:** **A4 · auto room detection** (walls → closed rooms via planar cycle detection — the larger pure-geometry Group-A payoff; the ear-clipping `triangulate` in E3/PR #26 is reusable once that merges), then **E2 · advisory checks** (advisory-only thresholds). Group C (angled/merged walls) remains the high-CSG-risk work for a run with runway. **Note for George:** four Phase 3 PRs now stack unmerged (#24 B2, #25 E1, #26 E3, this one D1) — none conflict, but merging them in order keeps the ledger honest for future runs.
## 2026-08-09 — Phase 3 · E3: Export the design — OBJ massing + lossless JSON (activate the dormant `ifc-export` seam)
- **Picked:** **E3**, per the pick protocol. Both recommended-next slices were already taken by **open PRs** — **PR #24** = B2 (per-slope roof overhang; edits `core/roofShape.js` + `build/geometry.js` + roof region of `main.js`) and **PR #25** = E1 (materials library; edits `core/model.js` + `edit/commands.js` + `app/inspector.js` + swatch region of `main.js`). E3 is the next non-overlapping slice in the recommended sequence and the one the plan explicitly frames as "a pure JSON/OBJ export before full IFC". Its substance is a **brand-new pure module** (`core/exportObj.js`) that touches **none** of the files those two PRs own, so there's no feature/region collision. *(Protocol honoured: `git fetch origin main` → branched clean off `origin/main` tip `6ab7294` = merged PR #23; `list_pull_requests(state=open)` → #24, #25 — checked file overlap, picked a disjoint slice; corrected the stale B1 ledger row (it merged as PR #23, not "PR open"); marked E3 `in-progress` in-branch before coding.)*
- **Why it's high value:** there was **no way to get a design out of the app at all** — no save/export button existed. E3 adds two: an **OBJ** geometry export (opens in Blender / SketchUp / CAD) and the project's own **lossless JSON save** (reloadable here). Novice-and-Pro useful, and it lights up a seam that had been dormant since Phase 1.
- **Engine-independent by construction (model/view separation intact — zero Three.js added).** All the geometry math is pure `core/exportObj.js`: `wallMesh` (each wall → a clean 8-vert / 12-tri box on its centreline — the same watertight primitive the CSG consumes), `roomSlabMesh` (each room floor → a thin slab whose caps are triangulated by a real **ear-clipping** `triangulate` that handles non-convex/L-shaped rooms), and `exportObj` (assembles valid Wavefront OBJ with per-element `o`/`usemtl` groups, 1-indexed faces, multi-storey base elevations) + `buildMtl` (a companion MTL coloured from the project's `materials` map via `hexToRgb01`). The separation guard (edit-core 30a–i) stays green — the new file is engine-free.
- **Honest scope (this is a design/visualization tool, not a fabrication spec).** The OBJ is a **massing** export: openings are **not** CSG-cut into the OBJ walls (that boolean lives in the Three.js view layer), and roof shells + external-GLB furniture are **not** baked in. All three are reported as `warnings` and echoed as `# note:` comments in the file, and the Export panel says so ("approximate massing export … not a fabrication or framing spec"). No code-compliance claims.
- **Zero save-format risk.** No new model field; `exportProjectJson` returns exactly the canonical `serialize(project)`. Running any export **mutates nothing** — proven byte-identical (pure test + headless).
- **Changed:**
  - `src/core/exportObj.js` — **new** pure module: `exportObj`, `wallMesh`, `roomSlabMesh`, `triangulate` (ear-clipping), `buildMtl`, `hexToRgb01`, `exportProjectJson`, `exportBaseName`.
  - `src/main.js` — Export… toolbar group wiring: a Pro-gated popover (`syncExportSeam` on the `ifc-export` seam, wired into the mode toggle + `__setMode` + boot), a `downloadText` Blob helper, and OBJ/JSON download handlers. New headless handles `__exportSeamVisible` / `__exportObj` / `__exportJson`. Imports the pure exporter.
  - `index.html` — `#export-group` toolbar button + `#export-panel` popover (two download actions + the honest-massing disclaimer) + CSS mirroring the roof/level/snap groups.
  - `src/test/export-obj.test.mjs` — **new** 47-check pure suite.
  - `automation/headless-verify/probes/e3-export.mjs` — **new** probe.
  - `docs/PHASE-3-PLAN.md` — ledger: E3 → PR open + headless VERIFIED; corrected the stale B1 row → Landed (PR #23).
- **Verified — pure (engine-independent, plain Node):** `node src/test/export-obj.test.mjs` **47/47** (hex→rgb; ear-clipping a square AND a non-convex L-shape with an area-tiling check; wall box 8v/12t at the right base+height; room slab caps+sides; OBJ structure/validity — v/f/o/usemtl/mtllib, faces 1-indexed in range, per-element groups; multi-storey base elevations; MTL per used material; JSON == lossless save + re-validates; **export mutates nothing**). Regression all green — `edit-core` **337/337** (separation guard 30a–i green), `gable-infill` **25**, `roof-ridge` **40**, `element-label` **41**, `room-hover-plan` **15**, `room-rename-plan` **18**, `phase2/test/phase2-core` **33**. **556 assertions, 0 fail.**
- **Verified — headless (real app, Playwright-CDP Chromium under swiftshader, vendored three r169 / csg v0.0.17 / meshbvh v0.7.6):** `node automation/headless-verify/verify.mjs phase3-export --probe probes/e3-export.mjs` → **19/19 PASS, zero console/page errors** + the Phase 1 regression (5 walls / 4 CSG openings / 2 rooms / GLB furniture / lossless save). Confirmed the Export group is **hidden in Simple / shown in Pro**; the live sample home exports to **7 objects (5 walls + 2 rooms), 56 vertices, 84 faces, 2 materials**, every face 1-indexed and in range, openings/roof/furniture flagged as warnings; the JSON export equals the canonical save and parses back to a schema-v1 project; the export left the save byte-identical; and the panel opens for the user. Also generated a real OBJ+MTL from the `sampleHome` template out-of-harness and eyeballed it — clean Wavefront (object names, metre coords, per-material `Kd` from the palette hex). Screenshot: `docs/verification/phase3-export.png`.
- **Not touched (no regression):** the Phase 1 model contract + lossless save (no new field; export is read-only and mutation-free); the phase2 core; CSG openings; GLB furniture; the roof / multi-level / measure / rooms work; model/view separation (the new module is engine-free by the automated guard). No overlap with the open PR #24 (roof) or PR #25 (materials) files.
- **Next:** **D1 · Import a plan** (enable the disabled tile — trace an uploaded floor plan as a plan-canvas underlay; pure screen↔world scale math + Canvas-2D, no Three.js), then **A4 · auto room detection** (the ear-clipping `triangulate` added here is reusable for it). Once PRs #24 (B2) and #25 (E1) merge, Group B is done and E's materials row is live; a natural E-group follow-on is **E2 · advisory checks** (strictly labelled approximate) and extending this exporter toward **IFC/glTF**.
## 2026-08-08 — Phase 3 · E1: materials library UI (activate the dormant `materials-swatch` seam)
- **Picked:** **E1**, the recommended-next slice in `PHASE-3-PLAN.md` (§ Recommended sequence #3). Group B's roof depth is closed on the pick side — **B1** gable-infill merged as **PR #23** (the ledger's "PR open" row was stale — corrected to Landed) and **B2** per-slope overhang is **open as PR #24** on `auto/2026-08-05`. Per the pick protocol I avoided B2's files (`core/roofShape.js`, `build/geometry.js`, roof-panel `main.js`) and chose the highest-value non-overlapping slice: **E1 activates `materials-swatch`**, a Simple-tier, novice-visible win that lets a user apply a surface finish (Warm white, Oak, Red brick, Slate, Sage, Navy…) to a selected wall or floor from the inspector. *(Pick protocol honoured: `git fetch origin main` → branched clean off `origin/main` tip `6ab7294` = merged PR #23; `list_pull_requests(state=open)` returned **only PR #24 (B2)** — no file overlap with E1. Marked E1 `in-progress` in-branch before coding, ledger row corrected for B1/B2.)*
- **No new save-field TYPE — it rides the model's existing `material` key.** Every wall/floor already carries a `material` key naming an entry in the project's `materials` map (Phase 1). E1 adds no field: `setElementMaterial` **repoints that existing key** at the chosen finish and, when the finish isn't yet in the map, **registers its definition on first use** so the render registry can resolve it. Undo is **byte-lossless in both directions** — it restores the prior key and, if THIS command added the finish's definition, deletes it again (adding then deleting a key restores the map's insertion order, so `serialize→deserialize→serialize` is byte-identical). Registering only on first use keeps saves minimal (unused library finishes are never stored), and the add/remove is strictly **LIFO with history**, so a finish shared by two elements survives undo of the second and is removed only when the first (which added it) is undone. Old saves stay byte-identical and still `validateProject()`.
- **Model/view separation intact — zero Three.js added.** The library (`MATERIAL_LIBRARY`), the command, the inspector slot and the builder are all pure `core`/`edit`/`app`; the view layer turns a chosen finish id into a real material through the **existing** `makeMaterialRegistry` + `pick()` path in `sceneBuilder.js` — **unchanged**, so `build/geometry.js` was NOT touched (which also keeps E1 clear of PR #24's geometry edits). The separation guard (edit-core 30a–i) stays green.
- **Changed:**
  - `src/core/model.js` — new pure **`MATERIAL_LIBRARY`** (10 frozen finishes: id + human label + color/roughness in the shape `makeMaterialRegistry` already consumes), **`materialDef(id)`** and **`isLibraryMaterial(id)`** lookups. `defaultMaterials()` unchanged (zero risk to existing saves/tests).
  - `src/edit/commands.js` — new **`setElementMaterial(levelId, kind, id, materialId, def)`** command (`kind` picks `findWall`/`findRoom`); registers the finish on first use, byte-lossless undo (restore key + remove-if-added).
  - `src/app/inspector.js` — new **`materialsSlot()`** (the swatch descriptor: current finish + options = the library, with the current finish prepended so a base-role default stays selectable), wired into the **wall** and **room** branches of `describeSelection` (an opening is a void, so **no** slot). New **`buildSetMaterial()`** builder — accepts a library finish or an id already in the map, `{ unchanged }` on a re-click, `{ error }` on a bad selection or unknown id.
  - `src/main.js` — `renderInspector` emits the swatch row (clickable finish buttons, current one marked `.sel`); new **`commitMaterial()`** dispatches through history + `refresh()` (re-derives the registry so the colour shows instantly); headless handle **`__setMaterial(id)`**.
  - `index.html` — swatch-row CSS (`.ins-swatches` / `.ins-swatch` / `.sel`).
  - `src/test/materials-swatch.test.mjs` — **NEW** pure suite (49 checks): command set/register/undo (incl. the LIFO shared-finish case + base-role no-add case); lossless round-trips + old-save-still-valid; `buildSetMaterial` set/unchanged/reject paths; `describeSelection` slot present for wall+room / absent for opening / both tiers; the seam registered once (Simple → both); no-geometry-touch; catalog integrity (unique ids, valid hex, frozen).
  - `automation/headless-verify/probes/e1-materials.mjs` — **NEW** probe driving the real swatch seam.
- **Verified:**
  - Pure: `materials-swatch` **49/49**. Regression all green — `edit-core` **337/337** (separation guard 30a–i green — no Three.js added to any pure module), `element-label` **41/41**, `gable-infill` **25/25**, `roof-ridge` **40/40**, `room-hover-plan` **15/15**, `room-rename-plan` **18/18**, `phase2-core` **33/33**. **558 assertions, 0 fail.**
  - Headless (real app, Playwright-CDP Chromium under swiftshader, vendored three r169 / csg v0.0.17 / meshbvh v0.7.6): **20/20 PASS, zero console/page errors** + the Phase 1 regression (5 walls / 4 CSG openings / 2 rooms / GLB furniture / lossless save). Confirmed the swatch slot renders in **both** Simple and Pro (11 swatches = 10 library + the current default), applying **Red brick** to a wall repointed the key + registered the finish (exactly one swatch reads selected), undo restored the key + removed the preset with the save still byte-lossless, and a **floor** took **Oak** through the same seam. Screenshot: `docs/verification/phase3-materials.png` (the Material row with two swatch rows, red-brick selected, the wall rendered red-brick on the plan).
- **Not touched (no regression):** the Phase 1 model contract + lossless save (E1 reuses the existing `material` key — old saves serialize byte-identically and still validate); the phase2 core; CSG openings; GLB furniture; the roof / gable-infill / multi-level / measure / room / rename / hover / label work; model/view separation (all new logic engine-free by the automated guard; `build/` untouched). PR #24's B2 files are untouched, so the two PRs don't collide.
- **Next:** **D1 · Import a plan** (enable the disabled tile — trace an uploaded floor plan as a plan-canvas underlay; pure screen↔world scale math + Canvas-2D underlay, no Three.js), then **A4 · auto room detection** (larger pure-geometry payoff). After PR #24 (B2) merges, Group B is fully done. Group C (angled/merged walls) remains the high-CSG-risk work for a run with runway.
## 2026-08-05 — Phase 3 · B2: per-slope roof overhang (eaves vs. rake — the roof can oversail the walls by different amounts on the sloped sides than at the ends)
- **Picked:** **B2**, the recommended-next slice (`PHASE-3-PLAN.md` § Recommended sequence #2, straight after B1 gable-infill **PR #23 merged**). Until now the roof overhang was a single uniform number — it oversailed the walls by the same amount on all four sides. Real pitched roofs distinguish the **eaves** (the two sloped sides, perpendicular to the ridge) from the **rake** (the gable/hip ends, along the ridge), which typically have a smaller overhang. B2 lets a designer set the two independently. *(Pick protocol honoured: `git fetch origin main` → branched clean off `origin/main` tip `6ab7294` = merged PR #23; `list_pull_requests(state=open)` returned **[] — no open PRs**, no file overlap. Marked B2 `in-progress` in-branch before coding.)*
- **Optional fields, lossless save.** Added OPTIONAL `eaveOverhang` / `rakeOverhang` to the roof. A pure `roofOverhangs(roof)` helper backfills **both** from the legacy uniform `overhang`, so a roof carrying only `overhang` yields `eave === rake === overhang` — its footprint and every downstream vertex are **byte-identical to pre-B2**, and an old save (no per-slope fields) still `validateProject()`s. `createRoof` only writes the two fields when explicitly set (so uncustomised saves keep the old shape); the `setRoofType` command carries them and its whole-roof snapshot undo restores a roof that never had them **cleanly** (proven byte-identical). `serialize→deserialize→serialize` stays identical after every edit.
- **Ridge axis resolved from the bare wall bounds — the key correctness point.** `roofFootprint` now expands the eave sides and the rake ends independently. Because an asymmetric expansion can flip which footprint axis is *longer*, resolving `'auto'` off the *expanded* box could flip the ridge; instead the ridge axis is resolved from the **wall bounds** (overhang-independent), and the view (`build/geometry.js`) passes a **concrete** `'x'`/`'z'` ridge to `roofSolid`/`gableInfill` so the footprint's per-slope expansion and the shell always agree. A larger **eave** widens the perpendicular span → **raises the ridge** (and lifts the gable-infill apex with it); a larger **rake** extends the ends **without** changing ridge height. Flat roofs keep their uniform overhang unchanged.
- **Model/view separation intact.** All new math is pure `core/roofShape.js` (`roofOverhangs`, per-slope `roofFootprint`); `build/geometry.js` gained only a concrete-ridge resolution. The separation guard (edit-core 30a–i) stays green with no allowlist change (`roofShape` is pure; `main.js` imports only the pure helper). B1's gable infill still meets the real ridge under per-slope overhang.
- **UI:** two Pro sliders ("Eaves" / "Rake", in cm) in the roof panel, shown only for pitched roofs (mirroring the pitch/ridge rows), reading/writing through the existing `roof-editor` seam — no new tier. Harness handles `__roof`/`__setRoofType` now expose the overhang fields for deterministic headless driving.
- **Changed:** `src/core/roofShape.js` (`roofOverhangs`, per-slope `roofFootprint`), `src/core/model.js` (`createRoof` optional fields + validation), `src/edit/commands.js` (`setRoofType` overhang patch), `src/build/geometry.js` (concrete ridge for roof + infill), `src/main.js` (eave/rake sliders + harness handles), `index.html` (two roof-panel rows + CSS), `src/test/roof-overhang.test.mjs` (new, 40 assertions), `automation/headless-verify/probes/b2-roof-overhang.mjs` (new), `docs/verification/phase3-roof-overhang.png` (new).
- **Verified:** pure `node src/test/roof-overhang.test.mjs` **40/40**; regression all green — edit-core **337**, gable-infill **25**, roof-ridge **40**, element-label **41**, room-hover **15**, room-rename **18**, `phase2/test/phase2-core.test.mjs` **33/33**. Headless (vendored three r169 / csg v0.0.17 / meshbvh v0.7.6, real app over CDP under swiftshader) **16/16, zero console errors** — probe drove baseline→big-eave (ridge 5.011→5.851 m)→big-rake (ridge stays 5.011 m, ends extend), Phase-1 5 walls / 4 openings / GLB / lossless intact. Screenshot `docs/verification/phase3-roof-overhang.png`.
- **Next:** **E1 · Materials library UI** (activate the dormant `materials-swatch` — a novice-visible Simple win over the existing `material` field) or **D1 · Import a plan** (wire the disabled tile). Group B (roof depth) is now complete (B1 infill + B2 overhang); Group C (angled/merged walls) remains the high-CSG-risk work to schedule with runway.

## 2026-08-03 — Phase 3 · B1: gable-end wall infill (the triangle under a gable reads as wall, not void)
- **Picked:** **B1**, the recommended-next slice in `PHASE-3-PLAN.md` (§ Recommended sequence #2, straight after Group A closed: A1 PR #20, A2 PR #21, A3 **PR #22 merged**). A gable roof left a triangular *void* at each end between the top of the walls and the two rising roof slopes — you saw straight through to the recessed, roof-coloured gable triangle of the roof shell, so the end read as roof/gap rather than wall. B1 fills it: a triangular **wall** panel at each gable end, at the wall plane, rising from eave height to the ridge. *(Pick protocol honoured: `git fetch origin main` → branched clean off `origin/main` tip `b230625` = merged PR #22; `list_pull_requests(state=open)` returned **[] — no open PRs**, no file overlap. A3's ledger row was stale ("PR open") — corrected to Landed (PR #22). Marked B1 `in-progress` in-branch before coding.)*
- **Zero new save field — it's derived geometry.** The infill is computed at build time from the existing roof (`type`/`pitch`/`ridge`/`overhang`) and the wall footprint; nothing new is stored, so old saves stay byte-identical and `serialize→deserialize→serialize` is unchanged. (Pure test 6c–6f prove a gable project still validates and round-trips losslessly, and computing the infill mutates nothing.)
- **Additive, zero-risk to the roof/CSG.** The roof shell (`roofSolid`) and its CSG-cut wall openings are **untouched**. The infill is a separate mesh in the **wall material**, so the roof keeps its own closing triangle at the overhang plane — which now correctly reads as the **rake overhang** above/around the wall infill. Model/view separation intact: all shape math is pure `core/roofShape.js`; only the existing view files gained a mesh builder. The separation guard (edit-core 30a–i) stays green.
- **Changed:**
  - `src/core/roofShape.js` — new pure `wallBounds(level)` (bare no-overhang wall box; `roofFootprint` refactored to reuse it, behaviour identical); new pure `gableInfill(type, roofFp, wallFp, {baseY,pitch,ridge,thickness})` → two solid triangular prisms (one per gable end, 8 tris each) on the wall plane, apex matched to the **actual** roof `ridgeY` so there's no gap under the peak; `hip`/`flat` yield no panels. New `DEFAULT_INFILL_THICKNESS` (0.12, = default wall thickness).
  - `src/build/geometry.js` — new `buildGableInfillMesh(level, material)` (view mesh, `kind:'wall'`, panel depth = the level's wall thickness); imports `DEFAULTS` from `core/model.js` for the fallback.
  - `src/build/sceneBuilder.js` — `buildGeometry` emits the infill mesh (wall material) alongside the roof for a gable-roofed level.
  - `src/test/gable-infill.test.mjs` — **new** pure suite (25 checks): wallBounds vs roofFootprint, prism count/finiteness, apex == roof ridge, panels inside the overhang, ridge-orientation placement, hip/flat = no infill, defaults, and the lossless/round-trip guarantees.
  - `automation/headless-verify/probes/b1-gable-infill.mjs` — **new** probe driving the roof-editor seam flat→gable→hip→gable against the real rebuilt scene graph.
- **Verified:**
  - Pure: `gable-infill` **25/25**. Regression green — `phase2-core` **33/33**, `edit-core` **337/337** (incl. the model/view separation guard), `roof-ridge` **40/40**, `element-label` **41/41**, `room-hover-plan` **15/15**, `room-rename-plan` **18/18**.
  - Headless (real app, Playwright-CDP Chromium under swiftshader, vendored three r169 / csg 0.0.17 / meshbvh 0.7.6): **14/14 PASS, zero console/page errors** + the Phase 1 regression (5 walls / 4 CSG openings / rooms / GLB furniture / lossless save). Confirmed a gable adds one wall-material infill mesh (16 tris = 2 gable ends) rising from the eave (y=2.7) to the ridge (y≈5.01); flat and hip add none; re-selecting gable restores it. Screenshot: `docs/verification/phase3-gable-infill.png`.
- **Next:** **B2 · per-slope eaves / overhang** (the other open roof follow-up) — extend the roof past the wall plane per slope, optional `overhang` already on the roof. Then **E1 · materials library UI** (activate the dormant `materials-swatch` row) and **D1 · import-a-plan** per the recommended sequence.

## 2026-08-02 — Phase 3 · A3: label a wall/opening from the inspector (Pro `element-label`)
- **Picked:** **A3**, the recommended-next slice in `PHASE-3-PLAN.md` (§ Recommended sequence #1, right after A2 which merged as **PR #21**). A3 extends the rename seam from rooms to **walls and openings**: a Pro user gives a selected wall/opening an **optional** free-text label ("Front door", "Party wall"). It's the smallest Group-A slice left and reuses the whole A1 room-rename shape — a single undoable command + an inspector builder — so it rides the existing seam with no new UI surface beyond one input row. *(Pick protocol honoured: `git fetch origin main` → branched clean off `origin/main` tip `0ee9055` = merged PR #21; `list_pull_requests(state=open)` returned **[] — no open PRs**, no file overlap. The A2 ledger row was stale ("PR open") — corrected to Landed (PR #21). Marked A3 `in-progress` in-branch before coding.)*
- **The one save-field question, handled the lossless way:** a wall/opening carries **no `label`** in the Phase 1 model, so A3 is the one place a save field is added — and it is **strictly optional**. Serialization is whole-object `JSON.stringify`, so an absent `label` never appears (old saves are byte-identical and still `validateProject()`), a set `label` round-trips for free, and **setting an empty label deletes the key** rather than storing `""`. The command tracks *whether the key existed* and **deletes on undo** when it didn't (assigning `undefined` would leak a phantom field and break the round-trip) — so label→undo is byte-identical in both directions.
- **Changed:** (model/view separation intact — **zero Three.js added**; all new logic is pure `core`/`edit`/`app`; the separation guard 30a–i stays green)
  - `src/edit/commands.js` — new **`setElementLabel(levelId, kind, id, label)`** command. `kind` picks the finder (`findWall` vs `findOpening`); `do()` records `had`/`prev` and sets or (on empty) deletes `label`; `undo()` restores the prior string or **deletes the key** when the element had none. Byte-lossless both ways.
  - `src/app/inspector.js` — `describeSelection` wall/opening branch now derives `typeName` + the current `label`, sets the **title to the label when present** (a named element reads by its name, like a room), and — **only when `isAvailable('element-label', mode)`** (Pro) — exposes a `rename` slot keyed to `label` with an "(optional)" placeholder (Simple keeps the clean read-only card). New **`buildElementLabel(project, selection, rawValue)`** mirrors `buildRoomRename` but **allows empty** (clears the label), returns `{ unchanged }` on a no-op blur, `{ error }` only for a non-wall/opening selection; trims/collapses/caps at new `MAX_ELEMENT_LABEL` (40).
  - `src/app/state.js` — registered **`'element-label': MODE.PRO`** (one row in `FEATURE_TIERS`; no scattered `if(pro)`).
  - `src/main.js` — the inspector rename input now **dispatches** on selection kind (`commitRename` → `commitRoomName` for a room, new `commitElementLabel` for a wall/opening); the `nameRow` template gained optional `placeholder` support; headless handle **`__labelElement(raw)`** commits + reports the resulting title/label. The A1 plan-editor path (`commitRoomName`) is unchanged.
  - `src/test/element-label.test.mjs` (**NEW** — own file per pick-protocol §5 so it never collides with in-flight test work) — **41 pure assertions**: the command (set wall/opening, overwrite, missing-element throws); **lossless undo** (undo deletes the key for a first label — byte-identical serialize — restores a prior label when one existed, redo/undo stable); **clearing** removes the key; **old saves stay valid + round-trip byte-identical** with and without labels; `buildElementLabel` trim/collapse/cap + set/clear/unchanged/reject paths; `describeSelection` Pro-gated slot + title-reflects-label; the seam registers once Pro-only; and labelling touches no walls/openings (metadata only).
  - `automation/headless-verify/probes/a3-element-label.mjs` (**NEW**) — the A3 probe for the committed harness.
  - `docs/PHASE-3-PLAN.md` — ledger: A2 → **Landed (PR #21)** (+ added A2/hover to the "already landed" table); A3 → **PR open (auto/2026-08-02), headless VERIFIED**; seam table A3 registry → `element-label`.
- **Verified — pure (engine-independent, plain Node):** `src/test/element-label.test.mjs` **41/41**; regression all green on the fresh base — `edit-core` **337/337** (separation guard 30a–i green — new test file correctly skipped by the scan; no Three.js added to any pure module), `roof-ridge` **40/40**, `room-rename-plan` **18/18**, `room-hover-plan` **15/15**, `phase2-core` **33/33**. **484 assertions, 0 fail.**
- **Verified — headless (Three.js, Chromium/swiftshader, the committed `automation/headless-verify/` harness):** `node automation/headless-verify/verify.mjs phase3-element-label --probe probes/a3-element-label.mjs` → **17/17 PASS, zero console/page errors.** Real app booted (5 walls / 4 CSG openings / 2 rooms / GLB furniture / lossless): in **Simple** a selected wall shows **no label field** (clean card, title "Wall"); in **Pro** the same selection exposes an **editable `label` slot** with an "(optional)" placeholder; committing **"Party wall"** wrote the model field, the **inspector title followed**, it was **one undoable command**, and **undo deleted the optional key** leaving the save **valid + lossless**; an **opening labelled through the same seam** ("Front door"). Screenshot `docs/verification/phase3-element-label.png` shows the inspector titled "Party wall" with the new Label field above the dimension inputs, the wall highlighted on the plan, and the 3D massing rendering beside it in Pro.
- **Not touched (no regression):** the Phase 1 model contract + lossless save (the `label` field is **optional** — old saves serialize byte-identically and still validate); the phase2 core; CSG openings; GLB furniture; the roof / multi-level / measure / room-area / plan-select / rename / hover work; model/view separation (all new logic is pure `core`/`edit`/`app`, engine-free by the automated guard). The A1 plan double-click room editor is unchanged.
- **Next:** per the recommended sequence, Group A is now done (A1–A3; A4 auto-room-detection is the larger remaining Group-A payoff) — move to **B1 · gable-end wall infill** (fill the triangle under a gable so the end reads as wall, not void; pure `roofShape` infill vertices + headless watertight check), then **B2 · per-slope eaves/overhang**. Alternatively **E1 · materials library UI** (activate the dormant `materials-swatch`) is a novice-visible Simple win off the existing `material` field.

## 2026-08-01 — Restore headless verification (root-caused the "Chromium reaping"), commit a reusable harness, and clear PR #21's A2 block
- **Picked:** the blocker itself, not a new feature. PR #21 (A2, still open) shipped on the pure proof only because *"Chromium would not launch in this session"* and its own note set the **next run's first task**: *restore headless verification and attach `phase3-room-hover-plan.png`* — flagging that the block *"would stall **every** view-layer slice."* Unblocking headless is strictly higher-value than any one slice, so that was the run. *(Pick protocol: `git fetch origin main`; `list_pull_requests(state=open)` → only PR #21 (A2). Checked out its branch `auto/2026-07-31` and completed its stated remaining work in-place — in scope for the PR, not a new/duplicate one.)*
- **Root cause (the 07-31 block was a launch-method problem, not an environment one):** Chromium is **not** reaped here. The 07-31 run drove it via the `chrome --screenshot …` CLI and bash-detached / `setsid` / raw-CDP `node`-spawn launches — all of which *are* reaped instantly in this sandbox (confirmed: `--screenshot` exits 0 but writes no PNG; GL falls back to `gl=none` which the env rejects — allowed impl is `egl-angle`). **Playwright's CDP launch is reaped-proof here** and brings up **WebGL 2.0 green under swiftshader, zero console errors.** The fix was the driver, not the sandbox.
- **Changed (all new files under `automation/` — outside `src/`, so the separation guard 30a–i is untouched; zero app code changed):**
  - `automation/headless-verify/verify.mjs` — generic driver: Playwright→vendored Chromium (`--use-gl=angle --use-angle=swiftshader --enable-unsafe-swiftshader --no-sandbox --disable-gpu-sandbox --disable-dev-shm-usage`), a static server that **mirrors Vite** (serves `public/` at root so `/sample.glb` resolves; empty-200 `/favicon.ico` so the browser's auto-probe doesn't trip the zero-errors gate), boots the real `src/main.js`, asserts the **always-on invariants** (zero console/page errors + Phase 1 regression: 5 walls / 4 CSG openings / rooms / GLB furniture / lossless), runs a pluggable per-slice `--probe`, screenshots to `docs/verification/<name>.png`, exit 0 iff all pass.
  - `automation/headless-verify/gen-meshbvh-generated.mjs` — the npm-free `preprocess` shim (regenerates meshbvh's 14 `*.generated.js` from `*.template.js`; `@if INDIRECT`/`@else`/`@endif` + `@echo INDIRECT_STRING`; **fails loudly** on any residual directive).
  - `automation/headless-verify/vendor-deps.sh` — one-command vendor + pin + generate + build (three **r169** · csg **v0.0.17** · meshbvh **v0.7.6** — the only combo compatible with the app's `package.json` pins; HEAD csg 0.0.18 needs three ≥0.179 and HEAD meshbvh 0.9.x imports `three/webgpu`+`three/tsl`).
  - `automation/headless-verify/build-verify-html.mjs` — injects the importmap into a copy of `index.html` (verify.html; gitignored scaffold).
  - `automation/headless-verify/probes/a2-room-hover.mjs` — the A2 probe (drives `__planHover` at a room centroid; asserts hover resolves the room + stays view-only).
  - `automation/headless-verify/README.md` — the recipe + the four expensive gotchas (CDP-not-CLI; pinned versions; `public/` at root; favicon 200).
  - `.gitignore` — add `/verify.html` (build scaffold). Vendored `/three`, `/three-bvh-csg`, `/three-mesh-bvh` stay ignored (cloned per run).
  - `docs/verification/phase3-room-hover-plan.png` — **the A2 screenshot PR #21 was waiting on.**
- **Verified — headless (the whole point):** `node automation/headless-verify/verify.mjs phase3-room-hover-plan --probe probes/a2-room-hover.mjs` → **12/12 PASS, zero console/page errors.** Real app booted: two 24 m² rooms, **4 CSG door/window openings cut in the 3D walls**, roof, and the **GLB furniture** all rendered; hover warms the room under the centroid (`room_a` "Living"), `__planHover` returns `undo:0` / `selection:null`, `__planHoverClear` clears, and `serialize()` is byte-identical across the hover sweep. Screenshot shows the warmer fill + orange stroke on the hovered room vs. the neutral neighbour. Harness re-run from clean (deps → generate → build → verify) to confirm it's reproducible.
- **Verified — pure (regression, fresh base):** `room-hover-plan` **15/15**, `edit-core` **337/337** (separation guard green), `roof-ridge` **40/40**, `phase2-core` **33/33** — **425 assertions, 0 fail.**
- **Not touched (no regression):** no `src/` change at all this run — A2's already-merged-to-branch code is unchanged; Phase 1 model + lossless save, phase2 core, CSG openings, GLB furniture, model/view separation all intact. The harness lives entirely under `automation/`.
- **Next:** **A2 (PR #21) is ready to merge** — headless is now green and the screenshot is attached; recommend George merges it. Then future view-layer slices just run `vendor-deps.sh` + `verify.mjs --probe …` (no more rediscovery). Next slice per sequence: **A3 · editable wall/opening label** (Pro; optional round-tripping field), then **B1 · gable-end wall infill**.

## 2026-07-31 — Phase 3 · A2: hover-highlight the room under the plan cursor *(headless verify BLOCKED by the environment — PR flagged)*
- **Picked:** **A2**, the recommended-next slice in `PHASE-3-PLAN.md` (§ Recommended sequence #1, right after A1 which merged as **PR #20**). It's the smallest, highest-reuse Group-A slice: **pure view state on pointer-move** — the plan warms the room under the cursor, no command, no history, no save touch — reusing the already-tested `roomAtPoint` (07-27). Un-tiered (both Simple and Pro), so it's a novice-visible affordance with zero seam risk. *(Pick protocol honoured: `git fetch origin main` → branched clean off `origin/main` tip `6db43d4` = merged PR #20; `list_pull_requests(state=open)` returned **[] — no open PRs**, no file overlap. Marked the ledger `in-progress` in-branch before coding.)*
- **Changed:** (model/view separation intact — **zero Three.js added**; the plan is Canvas-2D; the separation guard 30a–i stays green)
  - `src/app/planCanvas.js` — new **exported pure resolver `hoverRoomId(level, point, tool)`**: returns the room id to warm, or `null`; **gated to the SELECT tool** (hover never clutters wall-drawing / door-placing) and reuses `roomAtPoint` (smallest-containing-room-wins). Exported so the hover rule is unit-testable without a canvas. The closure now tracks a `hoveredId` view field; `updateHover(world)`/`clearHover()` recompute it (no command, no model touch); `drawRooms` gives a **hovered-but-not-selected** room a warmer fill + subtle stroke + warmer label. Pointer wiring: `pointermove` recomputes hover before redraw; `pointerleave` drops the highlight so no room stays warm off-canvas. Imports `roomAtPoint` (core/model) + `TOOL` (app/state).
  - `src/main.js` — headless handles `__planHover(x,z)` (drives a plan pointer-move, returns `{hover, undo, selection}` to prove it's view-only) and `__planHoverClear()`.
  - `src/test/room-hover-plan.test.mjs` (**NEW** — own file per pick-protocol §5 so it never collides with in-flight test work) — **15 pure assertions**: hover resolves the room under the cursor incl. smallest-room-wins + half-open corner miss; the **SELECT-tool gate** (no warm under DRAW_WALL/PLACE_DOOR/PLACE_WINDOW/MEASURE, warm restored under SELECT); null-safety (empty/null level, undefined tool); and the **view-only guarantee** (a sweep of hovers pushes nothing to `History` and leaves the `serialize()` save byte-identical). Hover is independent of selection.
  - `docs/PHASE-3-PLAN.md` — ledger: A1 → **Landed (PR #20)** (+ added to the "already landed" table); A2 → **PR open (auto/2026-07-31), headless BLOCKED**.
- **Verified — pure (engine-independent, plain Node):** `src/test/room-hover-plan.test.mjs` **15/15**; regression all green on the fresh base — `src/test/edit-core.test.mjs` **337/337** (separation guard 30a–i green — new file under `test/` correctly skipped by the scan; planCanvas stays Three.js-free), `src/test/roof-ridge.test.mjs` **40/40**, `phase2/test/phase2-core.test.mjs` **33/33**. `room-rename-plan.test.mjs` **18/18** unchanged.
- **Verified — headless: BLOCKED (environment).** Built the full Phase-0/1 harness in scratchpad — vendored **three r169** + **three-bvh-csg v0.0.17** + **three-mesh-bvh v0.7.6** via `git clone` (regenerated meshbvh's **14 `*.generated.js`** from `*.template.js` with a from-scratch `@if INDIRECT`/`@else`/`@endif` + `@echo INDIRECT_STRING` preprocess shim — npm/CDN blocked; **0 residual directives**), wrote the importmap harness over the real `src/main.js` and a raw-CDP driver (Node global `WebSocket`) under swiftshader. **Chromium would not launch in this session:** every method — bash foreground, `setsid`-detached, `node`-spawn `detached:true`, both the `chromium-1194/chrome` and `chromium_headless_shell-1194/headless_shell` binaries, `--use-gl=swiftshader` **and** `--disable-gpu`, Bash-tool sandbox on **and** off — reaped the parent process **instantly with zero log output** (empty `chrome.log`, no `DevTools listening` line, no coredump, nothing in `dmesg`); `--version` returns 0 but any full browser-process start kills the launcher (exit 144 = 128+16 in foreground, exit 1 detached). 16 GB free, 30 GB disk — **not resources**. This reads as an environment-level reaping of the Chromium process tree, not a code fault. Per the run's guardrails (*"if a check keeps failing, log the blocker + a proposed next step, push it on the branch, open the PR flagged blocked"*), I did **not** fabricate a screenshot.
- **Why shipping anyway is safe:** A2 touches **no engine code** — the plan is Canvas-2D, the separation guard confirms zero Three.js added, there is **no new geometry / no CSG / no save field**. The one impure part (a fill-color branch in `drawRooms` + two pointer handlers) is thin wiring around the **fully unit-tested** pure resolver; the model/selection/history are provably untouched. A regression here would have to be visual-only. The PR is flagged so a **headless render can follow** (screenshot `docs/verification/phase3-room-hover-plan.png`) once the env permits launching Chromium.
- **Not touched (no regression):** the Phase 1 model contract + lossless save (**no new save field** — hover is view state, never stored); the phase2 core; CSG openings; GLB furniture; the roof / multi-level / measure / room-area / plan-select / rename work; model/view separation (all new logic is pure `core`/`app`, engine-free by the automated guard).
- **Next:** **(0) run the A2 headless pass** and attach `phase3-room-hover-plan.png` when the environment can launch Chromium again — and, since this block would stall *every* view-layer slice, **investigate why Chromium is reaped here** (it worked in the 07-24…07-30 runs) so headless verification is restored. Then **A3 · editable wall/opening label** (Pro; extends the rename seam to walls/openings with an *optional* round-tripping field), then **B1 · gable-end wall infill**.

## 2026-07-30 — Phase 3 · A1: rename a room by double-clicking it on the plan (Pro `room-rename`)
- **Picked:** **A1**, the recommended-next slice in the new `PHASE-3-PLAN.md` (§ Recommended sequence #1) and the smallest / highest-reuse open row. It's the **plan-side entry** to the `renameRoom` command shipped 07-28 (PR #18) for the inspector — a real editing gesture (double-click a room, type its name) that reuses `roomAtPoint` (07-27) for the hit-test and `polygonCentroid` for label placement, both already-tested pure core functions. *(Pick protocol honoured: `git fetch origin main` → branched clean off `origin/main` tip `0a8ef5f` = merged PR #19; `list_pull_requests(state=open)` returned **[] — no open PRs**, no file overlap, no stale-base trap. Marked the ledger `in-progress` in-branch before coding.)*
- **Changed:** (model/view separation intact — zero Three.js added to any pure module; the separation guard 30a–i stays green)
  - `src/app/planCanvas.js` — the plan's `dblclick` handler now forwards the world point to an optional `onRoomActivate(world)` callback **after** `controller.finishChain()`, so a double-click that ends a wall chain never also triggers a rename. Pure view wiring — no model access, no geometry stored.
  - `src/main.js` — `beginPlanRoomRename(world)`: only acts under the **SELECT** tool; resolves the room via `roomAtPoint`, selects it (view state, no command), and — **only when `isAvailable('room-rename', mode)`** (Pro) — opens `openRoomLabelEditor(room)`, a floating `<input>` laid over `#plan-panel` at the room's `polygonCentroid`, pre-filled with the name. Enter/blur commits through the **existing** `commitRoomName` → `buildRoomRename` path (shared with the inspector, so identical trim/collapse/cap/validate + byte-lossless undo); Escape cancels with no history push. In Simple the room is still selected (its read-only readout shows) but the editor is gated shut with a "Switch to Pro to rename" hint. Added `__planDblClick` / `__roomEditor` / `__roomEditorCommit` / `__roomEditorCancel` headless handles. Imported `findLevel`, `roomAtPoint`, `polygonCentroid` from `core/model`.
  - `index.html` — `.plan-name-editor` style (a small centred chip floated over the plan via `transform: translate(-50%,-50%)`, positioned from the same `worldToScreen` mapping the plan renderer uses).
  - `src/test/room-rename-plan.test.mjs` (**NEW**, own file per pick-protocol §5 so it never collides with in-flight test work) — **18 pure assertions**: hit-test resolution (including *smallest-containing-room-wins* for a nested closet), centroid falls inside the room (editor placement), the shared `buildRoomRename` command applies + is byte-lossless on undo, input guards (empty rejected / unchanged no-op / cap at `MAX_ROOM_NAME` / non-room selection rejected), and no-new-save-field round-trip.
  - `docs/PHASE-3-PLAN.md` — ledger row A1 → **PR open (auto/2026-07-30)**.
- **Verified — pure (engine-independent, plain Node):** `src/test/room-rename-plan.test.mjs` **18/18**; `src/test/edit-core.test.mjs` **337/337** (separation guard green — new file lives under `test/`, correctly skipped by the scan); `src/test/roof-ridge.test.mjs` **40/40**; `phase2/test/phase2-core.test.mjs` **33/33**.
- **Verified — headless (Three.js, Chromium/swiftshader, the Phase 0/1 method):** vendored **three r169** (matching the pinned `^0.169.0`) + **three-bvh-csg v0.0.17** + **three-mesh-bvh v0.7.6** via `git clone` (regenerated meshbvh's **14 `*.generated.js`** from `*.template.js` with a from-scratch `@if INDIRECT`/`@else`/`@endif` + `@echo INDIRECT_STRING` preprocess shim — npm/CDN blocked), importmap harness over the **real** `src/main.js`, driven over raw CDP (Node global `WebSocket`) under swiftshader. Results — **16/16 checks, zero console errors, zero page errors.** The net-new proof: in **Simple** a plan double-click **selects** the room but **opens no editor** (Pro-gated); in **Pro** the same double-click **opens the inline editor pre-filled `"Living"`**; committing through the editor's **own Enter handler** renamed the model room to **"Great Room"** and the **inspector title followed**; an **empty name was rejected** (kept "Great Room"); **Escape cancelled** with no change; **undo restored the exact prior name AND a byte-identical model** (lossless); and the **Phase-1 regression held throughout** — 5 walls / 4 CSG openings / 2 rooms / **1 GLB furniture loaded** (the harness mirrors vite's `public/` root so `/sample.glb` renders) / scene meshes built / startup round-trip lossless. Screenshot: `docs/verification/phase3-room-rename-plan.png` (the floating "Great Room" editor over the room centroid on the plan, the inspector titled "Great Room" with its Name field + read-only area/perimeter, the 3D massing rendering beside it, Pro armed).
- **Next:** **A2 · hover-highlight the room under the plan cursor** (pure view state on pointer-move, no command, no save touch — reuses `roomAtPoint`), then **A3 · editable wall/opening label** (extends this rename seam to walls/openings with an *optional* round-tripping field). Both continue Group A's room/label momentum off a fresh base.

## 2026-07-29 — Phase 3: write `docs/PHASE-3-PLAN.md` — finally kill the root cause of the #13/#14/#15 collisions
- **Picked:** the **#1 recurring "Next"** — flagged in *five consecutive* runs (07-24 → 07-28) and named there as **the direct cause of the #13/#14/#15 duplicate-work / stale-base collisions**: there was **no Phase 3 plan**, so each morning's run re-picked overlapping work from the prior entry's vague free-text "Next" note. #13/#14 shipped the *same* room-area readout twice (one salvaged into a different slice); #15/#16 collided on a shared base — each burned most of a run on rebases instead of new value. Fixing this is higher-leverage than one more feature: it's the **root-cause reliability fix for this very automation**, and it's **zero-risk** (a planning doc can't regress a feature). *(Branched clean off `origin/main` tip `8c97a88` = merged PR #18; HEAD matched origin/main, **no open PRs** — no stale-ref/collision trap.)*
- **Changed:**
  - `docs/PHASE-3-PLAN.md` (**NEW**) — a grounded, sequenced Phase 3 plan mirroring the `PHASE-2-PLAN.md` shape: objective; the eight carried-forward **guiding constraints** (model/view separation, the Simple/Pro seam, the inviolable Phase 1 lossless save, command-based undo, watertight CSG, *design-tool-not-code-certification*, secrets server-side, headless-verify); a **"what already landed" ledger** mapping every Phase 3 DEV-LOG entry + merged PR (#13–#18) to its theme (rooms/measurement, roof, multi-level); a **deduplicated backlog** of the remaining work in **six dependency-ordered groups** (A rooms/labels · B roof depth · C wall/CSG depth · D onboarding tiles · E dormant Pro rows · F Meshy), each slice carrying tier, deps, new-save-field risk, and verification tier; a Simple/Pro exposure delta table; a recommended sequence; and — the actual point — a **Pick protocol** (fetch-fresh-base → check-open-PRs → pick-one → mark-the-ledger-before-coding → new-test-file-when-colliding → ship-only-if-verified) plus a **status ledger** (one row per slice, `todo → in-progress → Landed (PR #n)`) so runs stop re-picking taken work.
  - Grounded in a **codebase audit**, not guesswork: inventoried `src/`/`phase2/` trees, read the live `FEATURE_TIERS` (confirmed `materials-swatch`/`code-checks`/`ifc-export`/`furnish-photo-3d` are **registered but unwired**), and confirmed the *Import a plan* / *Outsource* tiles are the two **disabled** onboarding tiles at `src/main.js:602–603`.
- **Verified:** doc-only change — no code touched, so the appropriate proof is **no regression**. All three suites green on the fresh base: `node src/test/edit-core.test.mjs` → **337/0**, `node src/test/roof-ridge.test.mjs` → **40/0**, `cd phase2 && node test/phase2-core.test.mjs` → **33/33**. No headless run needed (no view-layer change). The model/view-separation guard is untouched and green.
- **Not touched (no regression):** zero product code — the Phase 1 model contract + lossless save, phase2 core, CSG openings, GLB furniture, roof/multi-level/measure/room work, and model/view separation are all byte-unchanged. This run adds only `docs/PHASE-3-PLAN.md` and this entry.
- **Next (now sequenced in the plan, not a vague note):** per the plan's recommended sequence, **A1 — rename a room by double-clicking its plan label** (Pro; reuses `roomAtPoint` + the `renameRoom` command; smallest, highest-reuse slice). Then A2 (hover-highlight) → A3 (wall/opening label) → B1/B2 (gable infill, eaves). **Every future run should open `docs/PHASE-3-PLAN.md`, follow the Pick protocol, and update the status ledger before coding.**

## 2026-07-28 — Phase 3: rename a room from the inspector (Pro) — the first *editable* room field
- **Picked:** the top "Next" flagged across the last four room runs — **"a room name as an editable field in Pro (rename the selected room from the inspector, an undoable command)."** Every prior room slice made a room *measurable* (area/perimeter readout → plan labels → whole-home GFA) and *selectable* (3D floor-click, then plan-click) — but its **name was still fixed** ("Living"/"Bedroom" from the template, never editable). This closes that gap with the cleanest shape: an **undoable `renameRoom` command over the room's existing `name` field** — a field `createRoom` already writes and `serialize` already round-trips, so it **adds no save field and can't touch the Phase 1 lossless contract**. Gated **Pro-only** (novice Simple keeps the clean read-only card; the numeric area/perimeter stay read-only in *both* tiers — only the name becomes an input). *(Branched clean off `origin/main` tip `c680917` = merged PR #17; HEAD matched origin/main, **no open PRs** — no stale-ref/collision trap.)*
- **Changed:**
  - `src/edit/commands.js` (pure — **zero Three.js/DOM**): `renameRoom(levelId, roomId, name)` — mirrors the existing `renameLevel`; captures the prior name for a byte-lossless undo, throws on a missing room. Imports `findRoom` (already in `core/model.js`).
  - `src/app/state.js`: registered **`'room-rename': MODE.PRO`** in `FEATURE_TIERS` — one row, read via the existing `isAvailable(tool, mode)` seam (no scattered conditionals).
  - `src/app/inspector.js` (pure — engine/DOM-free): the room descriptor now carries a **`rename` slot** (`{ key:'name', label:'Name', value }`) when `isAvailable('room-rename', mode)`, distinct from the numeric dimension fields (it commits a rename, not a parsed length). New export **`buildRoomRename(project, selection, raw)`** → `{command,name}` / `{unchanged:true}` (name equals current — caller skips a no-op history push) / `{error}` (non-room selection or empty name); the name is trimmed, interior whitespace collapsed, and capped at the new `MAX_ROOM_NAME` (40) so a label can't overrun the plan chip / inspector.
  - `src/main.js`: `renderInspector` renders the `rename` slot as a text input (`.ins-name`) above the read-only readout, wired to `commitRoomName` (Enter/blur → `history.execute(renameRoom)` → refresh; no re-validate/rollback needed — a name can't invalidate geometry). Added a small **`esc()` HTML-escaper** now applied to the inspector **title and the name value** — room names are user-typed, so a name like `<img onerror=…>` is rendered inert (a real XSS vector closed by this very edit path). Harness handle `__renameRoom(raw)` for headless driving.
  - `index.html`: an `.ins-name` style (wider, left-aligned, non-tabular) so the text input reads as a name, not a number.
  - `src/test/edit-core.test.mjs` — new **section 38** (20 assertions).
- **Verified — engine-independent:** `node src/test/edit-core.test.mjs` → **337 passed, 0 failed** (was 317; **+20** in section 38: `renameRoom` sets the name / undoes **byte-losslessly** / throws on a missing room; the descriptor shows **no rename slot in Simple**, an editable **Name field in Pro** seeded with the current name, and the **area/perimeter stay read-only even in Pro**; `room-rename` registers once, **Pro-only**; `buildRoomRename` trims+collapses+caps input, rejects empty/non-room selections, no-ops an unchanged name, preserves punctuation; and a **History** drive proving execute→rename, `serialize`+`validateProject` still pass with **no new room field** (save shape byte-identical keys), and **undo/redo are byte-lossless**). The **model/view-separation guard (30a–i) stays green** — all new logic is pure `core`/`edit`/`app`. Regression: `roof-ridge.test.mjs` **40/40**, `phase2/test/phase2-core.test.mjs` **33/33** — both unchanged.
- **Verified — headless (Three.js, Chromium/swiftshader, the Phase 0/1 method):** vendored **three r169** (matching the pinned `^0.169.0`) + three-bvh-csg **v0.0.17** + three-mesh-bvh **v0.7.6** via `git clone` (regenerated meshbvh's **14 `*.generated.js`** from `*.template.js` with a from-scratch `@echo`/`@if INDIRECT`/`@else`/`@endif` preprocess shim — npm/CDN blocked), importmap harness, drove the **real** `src/main.js` over raw CDP (Node's global `WebSocket`). Results — **18/18 checks, zero console errors, zero page errors.** The net-new proof: in **Simple** the selected room's card shows **no rename field**; switching to **Pro** reveals an editable **Name** input seeded `"Living"` (the DOM `.ins-name` really renders it) while area/perimeter stay read-only; `__renameRoom("Great room")` **changed the model room name and the inspector title**; a **markup name (`<img onerror=…>`) did not execute** in the panel (esc() hardening) yet is **stored verbatim in the model**; **undo restored** the prior name and a **no-op rename pushed nothing to history**; and the **Phase-1 regression held** throughout (5 walls / 4 CSG openings / 2 rooms / 1 GLB furniture / scene meshes / startup round-trip lossless). Screenshot: `docs/verification/phase3-room-rename.png` (the inspector titled **"Great room"** with the Name field + read-only area readout, the selected room warmer on the plan, the 3D massing rendering beside it, Pro armed).
- **Not touched (no regression):** Phase 1 model contract + lossless save (**no new save field** — rename only rewrites the room's existing `name`); the phase2 core; CSG openings; GLB furniture; the roof / multi-level / measure / room-area / GFA / plan-select work; model/view separation (all new logic is pure `core`/`edit`/`app`, engine-free by the automated guard). The read-only area/perimeter readout and the whole-home summary are unchanged — this only turns the name into an input in Pro.
- **Next:** natural extensions now that a room is renamable — **rename a room by double-clicking its plan label** (the label chip already draws at the centroid); an editable **wall/opening label** in Pro on the same seam; the recurring roof follow-ups (gable-end wall infill, per-slope eaves); wiring the **import-a-plan / outsource-drawing** onboarding tiles (still "coming soon"); and — flagged for many runs and the historical cause of the #13/#14/#15 base collisions — a **`PHASE-3-PLAN.md`** to sequence what's left so runs stop re-picking from vague "Next" notes.

---

## 2026-07-27 — Phase 3: plan-click room selection — make the plan's area-labelled rooms clickable
- **Picked:** the top "Next" flagged across the last three room-measurement runs — **"let a plan click select a room (point-in-polygon hit-test) — currently rooms are selectable only via the 3D floor."** Over the past four runs we built the per-room area readout, the plan-canvas centroid area labels, and the whole-home GFA summary — so the plan now *shows* every room's size, yet **clicking a room on the 2D plan did nothing** (a plan click only tested walls/corners; a miss set selection to `null` → the empty-state home summary). This slice closes that interaction gap with the cleanest possible shape: **pure point-in-polygon math + a selection fallback in the tool state machine, no Three.js and no model mutation** (selection is view state, so it can never touch the Phase 1 save contract). Un-tiered — available in **both Simple and Pro** (novice-first; a room is now clickable from either the plan or the 3D floor). *(No open PRs this run — branched clean off `origin/main` tip `e59ec8b` = merged PR #15; no stale-ref/collision trap.)*
- **Changed:**
  - `src/core/model.js` (pure — **zero Three.js/DOM**): `pointInPolygon(point, points)` (ray-casting / crossing-number test; winding-independent; `< 3`-point polygon contains nothing; the half-open `(zi>z)!==(zj>z)` edge convention avoids double-counting a grazed vertex; null-safe → false) and `roomAtPoint(level, point)` (the room whose floor polygon contains the plan point, **smallest-area containing room wins** so a nested/overlapping room resolves deterministically to the visually-innermost one; null-safe). Companions to the existing `polygonArea`/`polygonPerimeter`/`polygonCentroid`.
  - `src/edit/tools.js` — `_selectDown` gained a **room fallback**: the select precedence is now **vertex (corner-drag) → wall → room → none**. When no corner/wall is grabbed, a `roomAtPoint` hit sets `selection = { kind:'room', id }` — the **same** selection shape a 3D floor-click produces (`main.js:580`), so both surfaces agree. A click outside every room still clears the selection (→ whole-home summary). **No command, no history push, no rebuild** — selecting mutates nothing.
  - `src/main.js` — a `__planClick(x,z)` harness handle that drives a plan pointer-down through the real select→inspector path (mirrors the canvas `pointerdown` handler) and returns the resulting selection. No product-code change beyond the handle.
  - `src/test/edit-core.test.mjs` — new **section 37** (20 assertions).
- **Verified — engine-independent:** `node src/test/edit-core.test.mjs` → **317 passed, 0 failed** (was 297; **+20** in section 37: `pointInPolygon` inside/outside/far/reversed-winding/just-inside-corner/just-outside-edge/degenerate-<3-pt/null-safe; `roomAtPoint` resolving the sampleHome left half → Living, right half → Bedroom, an outside click → null, null-safe, and **nesting → smallest-area room wins**; a `ToolController` drive proving a plan click in a room interior selects it (`kind:'room'`), clicking the other room re-selects it, a click outside clears selection, a click **on a wall still selects the wall** (precedence), and the whole interaction pushes **no command / no rebuild** with the model **byte-identical** — the Phase 1 lossless save intact). The **model/view-separation guard (30a–30i) stays green** — `pointInPolygon`/`roomAtPoint` are pure `core`, and `tools.js` already imported from `core/model.js`, so the three-importer allowlist is unchanged. Regression: `roof-ridge.test.mjs` **40/40**, `phase2/test/phase2-core.test.mjs` **33/33** — both unchanged.
- **Verified — headless (Three.js, Chromium/swiftshader, the Phase 0/1 method):** vendored **three r169** (matching the app's pinned `^0.169.0`) + three-bvh-csg + three-mesh-bvh via `git clone` (regenerated meshbvh's **12 `*.generated.js`** from `*.template.js` with a from-scratch `@if INDIRECT`/`@else`/`@endif` + `@echo INDIRECT_STRING` preprocess shim — npm/CDN blocked), importmap harness, drove the **real** `src/main.js` over raw CDP (Node's global `WebSocket`). Results — **14/14 checks, zero console errors, zero page errors.** The net-new proof: with the SELECT tool active, `__planClick(-2, 0)` (Living-room interior) selected `{kind:'room', id:'room_a'}` and the **inspector rendered "Living · Floor area 24.0 m² · Perimeter 20.00 m"** — a plan click that previously did nothing; `__planClick(2, 0)` re-selected the Bedroom (`room_b`); `__planClick(0, 0)` **on the central partition still selected the wall** (`wall_5` — wall precedence preserved); `__planClick(20, 20)` cleared the selection; the undo stack stayed at **depth 0** and the save stayed **valid + lossless** across every click (view-only). The **Phase-1 regression held** throughout (5 walls / 4 CSG openings / 2 rooms / 1 GLB furniture). Screenshot: `docs/verification/phase3-plan-room-select.png` (the Living room fill warmer/selected on the plan, its area readout in the inspector, the 3D massing rendering beside it).
- **Not touched (no regression):** Phase 1 model contract + lossless save (**no new save fields** — selection is view state, computed on click, never stored; the hit-test issues no command); the phase2 core; CSG openings; GLB furniture; the roof / multi-level / measure / room-area / GFA work; model/view separation (all new logic is pure `core`/`edit`, engine-free by the automated guard). The 3D floor-click room selection is unchanged — this only adds the 2D-plan path to the same selection.
- **Next:** natural extensions now that a room is clickable from the plan — a **room name as an editable field in Pro** (rename the selected room from the inspector, an undoable command); highlighting the **hovered** room on the plan (not just the selected one); roof follow-ups still open (gable-end wall infill, per-slope eaves); wiring the **import-a-plan / outsource-drawing** onboarding tiles (still "coming soon"); and — flagged for many runs and the historical cause of the #13/#14/#15 base collisions — a **`PHASE-3-PLAN.md`** to sequence what's left so runs stop re-picking from vague "Next" notes.

## 2026-07-26 — Phase 3: roof ridge-direction override (gable & hip) — a roof follow-up, chosen to dodge an open-PR collision
- **Picked:** a **ridge-direction control** for pitched roofs. Until now `roofSolid` hard-coded the ridge along the longer plan axis (`W >= D`); this lets the designer force it **Auto / along-X / along-Z** from the roof panel. I picked it deliberately: **PR #15 (`auto/2026-07-25`, whole-home GFA summary) is still open and unmerged**, occupying `model.js` / `inspector.js` / `main.js` / `edit-core.test.mjs`. Ridge-direction is one of the "roof follow-ups" PR #15 *itself* flags as unbuilt next work, so there's **zero feature-duplication**, and its logic lives almost entirely in files #15 doesn't touch (`roofShape.js`, `commands.js`, `geometry.js`) — the two model/main touches sit in different regions and my tests are a **new file**, so the only real merge point with #15 is the DEV-LOG top (normal).
- **Stale-base trap caught:** the session's local `main` ref was **4 commits behind `origin/main`** (it lacked the merged PR #13/#14 room-area + centroid work). I branched off it by mistake, noticed the DEV-LOG top was wrong, then **rebased `auto/2026-07-26` onto `origin/main` (98a0588)** so this PR is purely additive and never reverts the merged room-area readout / plan labels. `edit-core` went 255 → 279 after the rebase, confirming #13/#14's assertions are back.
- **Changed:**
  - `src/core/roofShape.js` (pure — **zero Three.js/DOM**): `RIDGE_MODES` / `DEFAULT_RIDGE` exports and `resolveRidgeAlongX(footprint, ridge)` — `'auto'` = the old longer-axis pick, `'x'`/`'z'` force it, unknown → auto (never throws). `roofSolid` takes a `ridge` option, computes the slope run **perpendicular to the resolved ridge**, and returns `ridgeAlongX`. **Hip clamp:** the hip-end inset is clamped to the ridge-axis half-length, so forcing a hip ridge along the *shorter* axis collapses to a pyramid apex instead of self-crossing/inverting.
  - `src/core/model.js`: `DEFAULTS.roof.ridge='auto'`, `createRoof` carries `ridge`, and `validateProject` gates `roof.ridge` against `RIDGE_MODES` (optional — an old save with no ridge field still validates). No new *required* save field, so the Phase-1 lossless contract is untouched; ridge round-trips as a plain field.
  - `src/edit/commands.js`: `setRoofType` accepts `ridge` in its patch (undoable, byte-lossless — captures the whole prior roof).
  - `src/build/geometry.js`: passes `ridge: level.roof.ridge ?? 'auto'` into `roofSolid` (a one-line view-layer pass-through of the already-render-proven positions).
  - `src/main.js` + `index.html`: an **Auto / ↔ / ↕** ridge selector in the "Roof…" panel, shown+enabled only for gable/hip (hidden for flat), dispatching `applyRoof({ ridge })` through the same validate-and-rollback path as the type/pitch controls. Harness handles `__roof`/`__setRoofType` now report `ridge`.
  - `src/test/roof-ridge.test.mjs` — **NEW file** (40 assertions), kept out of `edit-core.test.mjs` so it composes with in-flight work.
- **Verified — engine-independent:** `node src/test/roof-ridge.test.mjs` → **40 passed, 0 failed** (`resolveRidgeAlongX` for auto/x/z + unknown-fallback; `roofSolid` omitting ridge is **byte-identical** to `ridge:'auto'` for both gable and hip; a forced ridge flips `ridgeAlongX` and changes the ridge height at the same pitch — auto gable ridge rises 2 m, forced-perpendicular rises 4 m at 45°; the hip pyramid clamp stays finite; `createRoof`/`validateProject`/`setRoofType` carry, gate, and losslessly edit+undo the ridge; serialize/deserialize round-trip). Regression: `edit-core` **279/279**, `phase2/test/phase2-core.test.mjs` **33/33** — both unchanged.
- **Verified — headless (Three.js, Chromium/swiftshader, the Phase 0/1 method):** vendored three r169 + three-bvh-csg v0.0.17 + three-mesh-bvh v0.7.6 via `git clone` (regenerated meshbvh's **14 `*.generated.js`** from `*.template.js` with a from-scratch `@if INDIRECT`/`@else`/`@endif` + `@echo INDIRECT_STRING` preprocess shim — npm/CDN blocked), importmap harness, drove the **real** `src/main.js` over raw CDP (Node's global `WebSocket`). Results — **15/15 checks, zero console errors, zero page errors.** The roof seam is **hidden in Simple, revealed in Pro**; a gable built one **non-indexed 8-triangle shell**; forcing **ridge:x vs ridge:z** rebuilt the roof with **different ridge heights in the actual scene** (maxY 4.61 m vs 5.18 m — the geometry really changed, not just the model field); the model ridge followed the control; **undo** restored the prior ridge; a **hip forced along the short axis stayed finite** (pyramid clamp, 6 tris); the model stayed **valid + lossless** across every ridge edit and the **Phase-1 regression held throughout** (5 walls / 4 CSG openings / 2 rooms / 1 GLB furniture). Screenshots: `docs/verification/phase3-roof-ridge-auto.png` (ridge along the longer axis) and `phase3-roof-ridge-forced.png` (ridge rotated 90°, a taller peak) — the "Roof…" seam armed in Pro in both.
- **Not touched (no regression):** Phase 1 model contract + lossless save (ridge is an optional plain field, no required schema change); the phase2 core; CSG openings; GLB furniture; the merged room-area / GFA-adjacent, multi-level, and measure work; model/view separation (all new logic is pure `core`/`edit`, engine-free by the automated guard).
- **Next:** the remaining roof follow-ups — **gable-end wall infill** (fill the triangle under a gable so it reads as wall, not void) and **per-slope eaves / overhang**; wiring the **import-a-plan / outsource-drawing** onboarding tiles (still "coming soon"); and — still unwritten after many runs and the direct cause of the #13/#14 and #15 base collisions — a **`PHASE-3-PLAN.md`** to sequence what's left so runs stop re-picking from vague "Next" notes.

## 2026-07-25 — Phase 3: whole-home floor-area (GFA) summary — fill the inspector's dead empty state
- **Picked:** the top "Next" from yesterday's room-area run — **a total floor-area readout across the whole home**. `formatArea` and the pure `polygonArea` already exist, and the per-room readout landed yesterday, so the natural next step is the aggregate: *"how big is my house?"* answered from the data. The cleanest home for it is the inspector's **empty state** — until now, clicking nothing showed a dead *"No selection — click a wall, opening or room"*. This slice turns that dead space into a **read-only whole-home summary** (total GFA + per-storey breakdown + room count), the lowest-risk possible shape: **pure polygon math + a read-only descriptor**, no Three.js, no model mutation, no command, no new save field (a measurement is computed on read, so it can never touch the Phase 1 save contract). Un-tiered — available in **both Simple and Pro** (novice-first). *(Branched from the true `origin/main` tip `4c69778` = PR #14; origin/main matched HEAD — no stale-ref trap.)*
- **Changed:**
  - `src/core/model.js` (pure — **zero Three.js/DOM**): `levelFloorArea(level)` (sum of a storey's room-polygon areas, delegating to `polygonArea` so it inherits the winding-independent / degenerate-safe → 0 behaviour; null-safe) and `projectFloorArea(project)` → `{ total, rooms, levels, byLevel: [{ id, name, area, rooms }] }` — the whole-home GFA plus a per-storey breakdown, computed on read, storing nothing.
  - `src/app/inspector.js` — `describeHomeSummary(project, { units })`: a **read-only** descriptor (shaped like the room readout so `main.js` renders it with the same row template) with a **Total floor area** field, a **per-storey area row for each storey** (only when the home has more than one storey — a single storey === the total, so no redundant row), and a **room count** row (`Rooms · storeys` when multi-storey). Follows the metric/imperial units toggle. An empty home reads 0 m² with a *"Draw a room to start measuring your home"* hint instead of dwelling on a bare 0.
  - `src/main.js` — the inspector's empty-state branch now falls back to `describeHomeSummary` (`describeSelection(...) || describeHomeSummary(...)`), so **nothing selected shows the home summary** instead of the dead "No selection" text. Added `__summary` / `__inspectorText` harness handles for headless driving. **No new Three.js**, no new gate — the summary is un-tiered.
  - `src/test/edit-core.test.mjs` — new **section 35** (18 assertions).
- **Verified — engine-independent:** `node src/test/edit-core.test.mjs` → **290 passed, 0 failed** (was 272; **+18** in section 35: `levelFloorArea` summing two rooms (24+24=48 m²), empty-storey and null → 0; `projectFloorArea` totalling GFA / room count / storey count, the per-storey breakdown carrying id/name/area/rooms, and **summing across two storeys** (48+24=72 m²); `describeHomeSummary` read-only + titled by the project name, the total-area field metric **and** imperial, **no per-storey rows for a single-storey home**, **per-storey rows for a multi-storey home** each carrying that storey's own area, `Rooms · storeys` formatting, the **empty-home "draw a room" hint**, computing the summary **mutating nothing** (lossless save byte-identical), and the real two-room sample home = 48 m² across 2 rooms). The **model/view-separation guard (30a–30i) stays green** — all new logic is pure `core`/`app`. Regression: `phase2/test/phase2-core.test.mjs` still **33/33**.
- **Verified — headless (Three.js, Chromium/swiftshader, the Phase 0/1 method):** vendored **three r169** (matching the app's pinned `^0.169.0`) + three-bvh-csg v0.0.17 + three-mesh-bvh v0.7.6 (regenerated its **14 `*.generated.js`** from `*.template.js` with a from-scratch `@if INDIRECT`/`@else`/`@endif` + `@echo INDIRECT_STRING` preprocess shim — npm/CDN blocked), importmap harness, drove the **real** `src/main.js` over raw CDP (Node's global `WebSocket`). Results — **11/11 checks, zero console errors, zero page errors.** With nothing selected the inspector shows the **whole-home summary** (descriptor read-only, `type:'home'`); **Total floor area rendered 48.0 m²** in the inspector DOM, the room row read **2**, and the dead **"No selection"** state is gone; switching to **imperial** re-formatted the whole readout to **sq ft**; the summary stayed **available + read-only in Pro** (un-tiered); and the **Phase-1 regression held** (valid + lossless; 5 walls / 4 openings / 2 rooms / **1 GLB furniture**; scene meshes built). Screenshot: `docs/verification/phase3-home-summary.png` (the "Sample two-room home · Total floor area 48.0 m² · Rooms 2" card beside the plan, the 3D massing rendering on the right).
- **Not touched (no regression):** Phase 1 model contract + lossless save (**no new save fields** — GFA is computed on read, never stored; the summary issues no command); the phase2 core; CSG openings; GLB furniture; the roof / multi-level / measure / room-area work; model/view separation (all new code is pure `core`/`app`, engine-free by the automated guard).
- **Next:** natural extensions now that both the per-room and whole-home readouts exist — a **plan-canvas area label** drawn at each room's centroid (see the number on the floor plan), a **room name as an editable field in Pro** (rename from the inspector), and a **per-storey GFA line in the status HUD**. Still open from prior runs: wiring the **import-a-plan / outsource-drawing** onboarding tiles (still "coming soon"), roof follow-ups (gable-end wall infill, ridge-direction override, per-slope eaves), and a **PHASE-3-PLAN.md** to sequence what's left.
- **Merge/rebase (post-open):** while PR #15 sat open, **PR #13 merged to main** (the plan-canvas room area labels — the very "plan-canvas area label at each room's centroid" I'd flagged as a Next, so no feature duplication; different slice). Merged `origin/main` into `auto/2026-07-25`: `src/core/model.js` auto-merged (its `polygonCentroid` and my `levelFloorArea`/`projectFloorArea` coexist); the only real conflicts were both PRs adding a **"section 35"** to the test suite and both prepending a DEV-LOG entry. Resolved by keeping #13's `polygonCentroid` section as **35** and **renumbering my whole-home-summary section to 36** (18 assertions relabelled 36a–36r), and keeping both DEV-LOG entries. Combined suite: `node src/test/edit-core.test.mjs` → **297 passed, 0 failed** (272 base + 7 from #13 + 18 mine); `phase2/test/phase2-core.test.mjs` still **33/33**.
- **Merge/rebase (post-open, 2nd):** while PR #15 stayed open, **PR #16 (`auto/2026-07-26`, roof ridge-direction override) merged to main**. Merged `origin/main` in again: `src/core/model.js` (its new `ridge` field) and `src/main.js` auto-merged with my summary code; #16 kept its roof tests in a **new file** (`roof-ridge.test.mjs`), so my `edit-core` **section 36** didn't collide. Only the DEV-LOG top conflicted. Combined suites after this merge: `edit-core` **297/0**, `roof-ridge` **40/0**, `phase2` **33/33**.

## 2026-07-24 (session 2) — Phase 3: plan-canvas room area labels — salvaged from a duplicate-work collision
- **Context (collision):** the 2026-07-23 run opened **PR #13** ("room floor-area & perimeter readout") and it sat unmerged. The next morning's run, unaware #13 was still open, picked the **same** feature from the measure-tool "Next" note and merged it as **PR #14** (this branch's base `4c69778`). So #13 became a near-duplicate of already-merged work and no longer merged cleanly. Rather than force a redundant duplicate through (a regression is worse than no change), I **reset `auto/2026-07-23` onto the new main and reworked PR #13 into the clean, additive follow-up that PR #14 itself flagged as a "Next": a plan-canvas area label at each room's centroid.** PR #14 delivered the *inspector* readout; this delivers the *plan overlay* it didn't build. No duplication remains.
- **Changed (all on top of PR #14):**
  - `src/core/model.js` — `polygonCentroid(points)` (NEW, pure), the companion to PR #14's `polygonArea`/`polygonPerimeter`: area-weighted centroid with a vertex-average fallback for a degenerate/collinear polygon (never NaN/∞), so a label never lands off-plan. **Zero Three.js/DOM.**
  - `src/app/planCanvas.js` — `drawRooms(lv)`: a subtle per-room floor fill (the selected room reads warmer with a highlight stroke) + an **area-label chip at the centroid** in the active units (`formatArea`). Drawn **under** the walls so wall centerlines stay crisp. Reads the model + derives area/centroid on the fly; stores no geometry, still Canvas-2D and three-free. This surfaces PR #14's read-only readout on the plan so **every** room shows its size at a glance, not just the selected one.
  - `src/test/edit-core.test.mjs` — new **section 35** (7 assertions) for `polygonCentroid`.
- **Verified — engine-independent:** `node src/test/edit-core.test.mjs` → **279 passed, 0 failed** (was 272 after PR #14; **+7** in section 35: centroid of a square, an **L-shape** area-weighted centroid pulled toward the bulk (not the bbox centre), winding-independence, `< 3`-point vertex-average + empty-origin + collinear finite fallbacks, and the real `sampleHome` room centroid landing within its bounds). The **model/view-separation guard stays green** (`polygonCentroid` is pure `core`; `planCanvas.js` stays three-free). Regression: `phase2/test/phase2-core.test.mjs` still **33/33**.
- **Verified — headless (Three.js, Chromium/swiftshader, the Phase 0/1 method):** vendored three r169 + three-bvh-csg 0.0.17 + three-mesh-bvh via `git clone`, importmap harness, drove the **real** `src/main.js` over raw CDP. Results — **14/14 checks, zero console errors, zero page errors.** The **net-new proof:** sampling the plan canvas at each room's centroid found a bright area-label chip for **2/2 rooms** (both the two-room starter's rooms are labelled, not just the selected one); PR #14's inspector readout still reads **Living · 24.0 m² · 20.00 m** (258 sq ft imperial), read-only; selecting a room stays byte-lossless; Phase-1 regression intact (5 walls / 4 CSG openings / 2 rooms / 1 GLB furniture). Screenshot: `docs/verification/phase3-room-area-plan.png` (both rooms fill-tinted with **24.0 m²** centroid labels, the selected Living room warmer, PR #14's inspector reading area+perimeter beside it).
- **Not touched (no regression):** Phase 1 model contract + lossless save (`polygonCentroid` is derived-only, no stored fields, no command); PR #14's inspector readout (unchanged — this only adds the plan overlay); the phase2 core; CSG openings; GLB furniture; roof/multi-level/measure work; model/view separation (all new code pure `core` + Canvas-2D `app`, engine-free by the automated guard).
- **Next (still open, dedup-aware):** a **total floor-area** sum across all rooms (whole-home GFA, per storey) — PR #14 flagged this too; let a **plan click select a room** (point-in-polygon hit-test — currently rooms are selectable only via the 3D floor); a per-room **name-edit** in Pro; roof follow-ups (gable-end infill, ridge-direction override, per-slope eaves); import-a-plan / outsource onboarding tiles; and a **PHASE-3-PLAN.md** to sequence work so two runs don't collide on the same slice again.

## 2026-07-24 — Phase 3: room floor-area + perimeter readout — fill the empty room-selection inspector
- **Picked:** the top natural follow-up flagged by the measure-tool run — **an area readout for a selected room** (`formatArea` already existed in `units.js`, unused). A floor is *already selectable* (a 3D pick maps `kind:'floor'` → `selection {kind:'room'}` in `main.js`, and floor meshes are stamped `kind:'floor'/modelId:room.id`), but `describeSelection` returned **null** for a room, so clicking a room just showed the empty "No selection" state. This slice fills that gap with the cleanest possible shape: **pure polygon math + a read-only inspector descriptor**, no Three.js and **no model mutation** (a measurement is view state, so it can never touch the Phase 1 save contract). A floor-area readout is table-stakes for any design app and is universally useful, so it's available in **both Simple and Pro** (a novice-friendly readout, never an editable input). *(Branched from the true `origin/main` tip `d97580e` = PR #12; origin/main matched HEAD — no stale-ref trap.)*
- **Changed:**
  - `src/core/model.js` (pure — **zero Three.js/DOM**): `polygonArea(points)` (shoelace, **winding-independent**, degenerate-safe → 0 for < 3 points so a readout never shows negative/NaN) and `polygonPerimeter(points)` (closed-polygon edge sum); plus a `findRoom(level, id)` lookup beside `findWall`/`findOpening`.
  - `src/app/inspector.js` — `describeSelection` now handles `selection.kind === 'room'`: `locate()` gained a room branch, and a selected room returns a **read-only** descriptor (`editable:false`, `type:'room'`, titled by the room's name) with a **Floor area** field (`formatArea`) and a **Perimeter** field (`formatLength`), both following the metric/imperial units toggle. Also **guarded `buildDimensionEdit`** to refuse a room selection (`{ error: 'Room measurements are read-only' }`) so the readout can never build a bogus resize command — the readout carries **no command** and issues **no history entry**.
  - `src/main.js` — the inspector already renders any `editable:false` descriptor as a static readout, so wiring was minimal: the footer now honours an optional `desc.hint` (rooms show "Floor area updates automatically as you edit the room" instead of the wall/opening "Switch to Pro to edit exact dimensions"), and the empty-state copy now reads "click a wall, opening or room". **No new Three.js**, no new gate — the room readout is un-tiered.
  - `src/test/edit-core.test.mjs` — new **section 34** (17 assertions).
- **Verified — engine-independent:** `node src/test/edit-core.test.mjs` → **272 passed, 0 failed** (was 255; **+17** in section 34: `polygonArea`/`polygonPerimeter` on a unit square, a 4×6 rectangle (24 m²/20 m), **winding-independence** (CW = same positive area), a right triangle, and **degenerate-safe** 0 for < 3 points; the room descriptor titled by name, **read-only in Simple AND Pro**, area+perimeter formatted metric **and** imperial (following the units toggle), `findRoom`, `buildDimensionEdit` **refusing** a room (no command), an unknown room id → null, the `sampleHome` rooms describing (Living = 24 m²), and **describing a room mutating nothing — the lossless save byte-identical**). The **model/view-separation guard (30a–30i) stays green** — all new logic is pure `core`/`app`, the three-importer allowlist is unchanged. Regression: `phase2/test/phase2-core.test.mjs` still **33/33**.
- **Verified — headless (Three.js, Chromium/swiftshader, the Phase 0/1 method):** vendored **three r169** (matching the app's pinned `^0.169.0` — cloning HEAD first produced WebGL shader-validation errors + a blank 3D panel, so I pinned to r169) + three-bvh-csg v0.0.17 + three-mesh-bvh v0.7.6 (regenerated its **14 `*.generated.js`** from `*.template.js` with a from-scratch `@if INDIRECT`/`@else`/`@endif` + `@echo INDIRECT_STRING` preprocess shim — npm/CDN blocked), importmap harness, drove the **real** `src/main.js` over raw CDP (Node's global `WebSocket`). Results — **zero console errors, zero page errors.** The scene's floor mesh is stamped `kind:'floor'/modelId:'room_a'` and **matches a real room id** (the pick target the 3D floor-click resolves to); selecting the room rendered **Living · Floor area 24.0 m² · Perimeter 20.00 m** in the inspector DOM (Simple); switching to **Pro + imperial** re-formatted to **258 sq ft · 65′ 7″** and stayed **read-only**; a wall selection still showed **editable** length/thickness/height (no regression); empty selection → null; and the **Phase-1 regression held** (valid + lossless; 5 walls / 4 CSG openings / 2 rooms / **1 GLB furniture**). Screenshot: `docs/verification/phase3-room-area.png` (the "Living" area readout beside the plan, the 3D massing rendering on the right).
- **Not touched (no regression):** Phase 1 model contract + lossless save (**no new save fields** — area/perimeter are computed on read, never stored; the readout issues no command); the phase2 core; CSG openings; GLB furniture; the roof / multi-level / measure work; model/view separation (all new code is pure `core`/`app`, engine-free by the automated guard).
- **Next:** natural extensions now that the readout exists — a **total floor-area** sum across all rooms (a whole-home GFA readout, per storey), showing the **room name as an editable field in Pro** (rename from the inspector), and a **plan-canvas area label** drawn at each room's centroid. Still open from prior runs: wiring the **import-a-plan / outsource-drawing** onboarding tiles (still "coming soon"), roof follow-ups (gable-end wall infill, ridge-direction override, per-slope eaves), and a **PHASE-3-PLAN.md** to sequence what's left.

## 2026-07-22 (session 2) — Phase 3: the measure tool — the last unwired Pro-seam row, activated
- **Picked:** with the gable/hip roof slice merged (PR #11), the **`measure-tool` Pro-seam row** was the last `FEATURE_TIERS` entry still defined-but-unwired (`MODE.PRO` since S1) — the same dormant-seam shape the previous five Pro rows each had before being surfaced. A ruler is high-value and expected in any design app, and it's the cleanest possible slice: **pure distance math + a 2D-canvas overlay, no Three.js, and no model mutation** (measuring is view state, so it can never touch the Phase 1 save contract). *(Branched from the true `origin/main` tip `6b5fe3d` = PR #11; origin/main matched HEAD — no stale-ref trap.)*
- **Changed:**
  - `src/edit/measure.js` (NEW, pure — **zero Three.js/DOM**): `measureDistance(a,b)`, `measureMidpoint(a,b)`, and `describeMeasure(measure, units)` → `{ meters, label, mid, complete }` (label via the existing `units.js` `formatLength`, so metric/imperial follow the display-units toggle). Node-testable; importing it under Node is itself the separation guard.
  - `src/app/state.js` — added `TOOL.MEASURE`.
  - `src/edit/tools.js` — the ruler state machine on `ToolController`: `_measure` **view-state** (`{from, to, complete}`), `_measureDown` (first click sets the anchor, the second — vertex/angle-snapped to the anchor — completes it, a third restarts), live `pointerMove` preview, and a `measureSegment()` accessor for the renderer. **No command, no history push, no rebuild** — measuring mutates nothing. Snapping is reused, so a click near a corner lands exactly on it (measure a wall precisely). `TOOL_FEATURE[MEASURE] = 'measure-tool'`.
  - `src/app/planCanvas.js` — `drawMeasure()`: a dimension line (dashed while placing, solid once committed) with perpendicular end ticks and a rounded distance chip at the midpoint, formatted in the active units. Reads controller view-state only; still no Three.js, still stores no geometry.
  - `src/main.js` + `index.html` — a Pro-only **"Measure"** toolbar button gated on `isAvailable('measure-tool', mode)`; `syncMeasureSeam()` hides it in Simple and, on leaving Pro, drops any active ruler back to Select so the seam can't strand a hidden tool. Harness handles `__setTool/__measure/__measureClick/__measureSeamVisible`.
- **Verified — engine-independent:** `node src/test/edit-core.test.mjs` → **255 passed, 0 failed** (was 239; **+16** in a new section 33: `measureDistance`/`measureMidpoint`/`describeMeasure` metric+imperial+incomplete, and a full `ToolController` drive proving two clicks measure, a third restarts, live preview tracks the pointer, **the model stays byte-identical with an empty undo stack** (measuring mutates nothing), switching tools drops the ruler, and the Pro-only seam). The **model/view-separation guard stays green** — `measure.js` is pure and the three-importer allowlist is unchanged. Regression: `phase2/test/phase2-core.test.mjs` still **33/33**.
- **Verified — headless (Three.js, Chromium/swiftshader, the Phase 0/1 method):** reused the session's vendored three.js + three-bvh-csg + three-mesh-bvh (generated files intact), importmap harness, drove the **real** `src/main.js` over raw CDP. Results — **13/13 checks, zero console errors, zero page errors.** The Measure button is **hidden in Simple, revealed in Pro**; activating the tool and clicking (0,0)→(4,0) read **exactly 4.00 m**; the model stayed **byte-identical and lossless with an empty undo stack** across the measurement (view-only proof); leaving Pro **dropped the ruler and reverted to Select**; Phase-1 regression intact (5 walls / 4 CSG openings / 2 rooms / 1 GLB furniture). Screenshot: `docs/verification/phase3-measure-tool.png` (the orange 4.00 m dimension line + chip across the interior wall, HUD "undo 0").
- **Not touched (no regression):** Phase 1 model contract + lossless save (**the measure tool adds no model fields and issues no commands** — it is pure view state); the phase2 core; CSG openings; GLB furniture; the roof work from PR #11; model/view separation (all new logic is pure `core`/`edit`/`app` code, engine-free by the automated guard).
- **Next:** every `FEATURE_TIERS` Pro-seam row is now wired. Remaining Phase 3 depth: wiring the **import-a-plan / outsource-drawing** onboarding tiles (still "coming soon"); roof follow-ups (**wall-material gable-end infill**, a **ridge-direction override**, per-slope eaves); and a natural measure follow-up — an **area readout** for a selected room (the pure `formatArea` already exists in `units.js`). A **Phase 3 plan doc** (like `PHASE-2-PLAN.md`) would also help sequence what's left.

## 2026-07-22 — Phase 3, roof types beyond flat: gable + hip — the dormant `roof-editor` Pro-seam row, activated
- **Picked:** the top-listed "Next" from yesterday's multi-level entry — **roof types beyond flat (gable/hip)**. `roof.type` has existed in the model since Phase 1 (`createRoof` carried `'flat'` with a "gable/hip come in a later phase" comment) and `FEATURE_TIERS['roof-editor'] = MODE.PRO` has been defined-but-unwired since S1 — the exact dormant-seam shape the last four Pro rows (exact-dimensions, per-opening sizing, snapping, multi-level) each were before being surfaced. This slice turns the latent `roof.type` field into a real, editable feature: pitched-roof **shape math in the pure core**, an undoable **command**, a thin **view builder**, and a **Pro-only** roof panel. *(Branched from the true `origin/main` tip `1da51bd` = PR #10; origin/main matched HEAD — no stale-ref trap this run.)*
- **Changed:**
  - `src/core/roofShape.js` (NEW, pure — **zero Three.js/DOM**): the gable/hip geometry lives here so it's unit-tested under Node and the model stays the single source of truth. `roofFootprint(level)` (wall bbox + overhang), `pitchRise(run, deg)`, and `roofSolid(type, footprint, {baseY, pitch})` → a **closed, outward-facing, non-indexed triangle shell** for gable (2 slopes + 2 gable ends + bottom cap) or hip (2 trapezoids + 2 hip ends + bottom; a **square footprint degenerates to a pyramid**, zero-area faces dropped). Ridge runs along the longer plan axis. Winding is fixed by a centroid-based `orientOutward` pass (valid because the shells are convex) so the view layer never worries about face order. Exports `ROOF_TYPES`, `DEFAULT_ROOF_PITCH` (30°), `isPitched`.
  - `src/core/model.js` — `createRoof` gains a `pitch` field (default 30°, only affects gable/hip); `DEFAULTS.roof.pitch`; `validateProject` now rejects an unknown `roof.type` and an out-of-range pitch (0<pitch<90) on a pitched roof. **No forced migration** — `deserialize` still passes projects through untouched, so an old save without `pitch` stays byte-identical until edited (lossless guarantee preserved).
  - `src/edit/commands.js` — `setRoofType(levelId, {type, pitch})`: an undoable command that changes only the roof's shape/pitch (thickness/overhang/material carry through), captures the whole prior roof for a **byte-lossless undo**, and backfills the default pitch when switching a pitch-less roof to gable/hip.
  - `src/build/geometry.js` — `buildRoofMesh` now branches: **flat = the Phase-1 slab, unchanged**; gable/hip = a `BufferGeometry` built from `roofSolid`'s positions + `computeVertexNormals` (the ONLY new Three.js in the slice, correctly inside the render allowlist). `userData.kind` stays `'roof'` so the S5 cutaway still hides it.
  - `src/main.js` + `index.html` — a **Pro-only** "Roof…" popover (Flat / Gable / Hip buttons + a pitch slider that disables for flat) behind the single `isAvailable('roof-editor', mode)` gate, mirroring the snap/level panels. It targets the **roof-bearing (top) storey**, re-validates and rolls the command back on any invalid edit, and `refresh()` re-renders it so undo/redo of a roof edit reflects live. Harness handles `__roof/__setRoofType/__roofSeamVisible`.
- **Verified — engine-independent:** `node src/test/edit-core.test.mjs` → **239 passed, 0 failed** (was 215; **+24** in a new section 32: `ROOF_TYPES`/`isPitched`/`pitchRise`; `roofFootprint` null + overhang expansion; gable = 8 triangles with the ridge at eave+run·tan(pitch) and every vertex inside the footprint; hip = 8 triangles over a rectangle but a **6-triangle pyramid with an apex over the centre** for a square; `setRoofType` do/undo/**redo** byte-lossless, thickness/overhang untouched, pitch backfill, throws with no roof; validation of bad type + out-of-range pitch; the Pro-only seam). The **model/view-separation guard (30a–30i) stays green** — `roofShape.js` is pure and the importer allowlist is unchanged (only `build/geometry.js`, already listed, gained the roof math). Regression: `phase2/test/phase2-core.test.mjs` still **33/33**.
- **Verified — headless (Three.js, Chromium/swiftshader, the Phase 0/1 method):** vendored three.js + three-bvh-csg + three-mesh-bvh via `git clone` (regenerated meshbvh's **12 `*.generated.js`** from `*.template.js` with a from-scratch `/* @if INDIRECT */`·`@else`·`@endif` + `/* @echo INDIRECT_STRING */` preprocess shim — npm/CDN blocked), importmap harness, drove the **real** `src/main.js` over raw CDP (Node's global `WebSocket`). Results — **16/16 checks, zero console errors, zero page errors.** The seam is **hidden in Simple, revealed in Pro**; the default roof is one **indexed slab** (flat, unchanged); switching to **gable** produced a single **non-indexed 8-triangle pitched shell** with its ridge at **maxY 5.01 m** (well above the 2.7 m walls); **hip** rendered a lower **4.61 m** hipped shell; **undo** returned the roof to flat; the model stayed **valid + lossless** after every switch and the **Phase-1 regression held throughout** (5 walls / 4 CSG openings / 2 rooms / 1 GLB furniture). Screenshots: `docs/verification/phase3-roof-gable.png` (two slopes to a ridge) and `phase3-roof-hip.png` (four hipped slopes) — the "Roof…" seam armed in Pro in both.
- **Not touched (no regression):** Phase 1 model contract + lossless save (the only new field, `roof.pitch`, is additive and never force-injected on load); the phase2 core + contract doc; CSG wall openings; GLB furniture loading; the multi-level storey work (the roof still caps the top storey — the panel finds the roof-bearing level); model/view separation (all new shape math is pure `core/`, proven engine-free by the automated guard; the sole new Three.js is inside the existing `build/` allowlist).
- **Next:** more Phase 3 depth behind the seam — wiring the **import-a-plan / outsource-drawing** onboarding tiles (still "coming soon"), and smaller roof follow-ups now that the shape math exists: a **per-slope overhang / eave-height** control, **gable-end infill** so the triangle under a gable reads as wall not void, and a **ridge-direction override** (currently auto = longer axis). A **measure tool** (the last unwired Pro row) is also open.

## 2026-07-21 — Phase 3, first slice: multi-level (storey) editing — the dormant `multi-level` Pro-seam row, activated
- **Picked:** With Phase 2 sealed (`PHASE-2-COMPLETE.md`), its own recommendation was Phase 3 depth "behind the existing seam." The clearest, most self-contained first slice was the **`multi-level` Pro-seam row** — `FEATURE_TIERS['multi-level'] = MODE.PRO` has been defined since S1 but was never wired, exactly the dormant-seam shape the last three Pro rows (exact-dimensions, per-opening sizing, snapping) each were before being surfaced. The model has *always* carried `Levels[]` and the scene builder already loops **every** level and stacks it by `elevation` — but the app hard-pinned `project.levels[0]` in five places, so a user could never add or edit a second storey. This slice adds the storey **commands + active-level concept + a Pro-only switcher UI**, turning existing latent capability into a real feature with **zero** scene-builder changes. *(Branched from the true `origin/main` tip `7222e05` = PR #9; origin/main matched HEAD — no stale-ref trap this run.)*
- **Changed:**
  - `src/core/model.js` — `stackElevations(levels)` (NEW, pure): anchors the ground storey at 0 and stacks each storey contiguously on the one below (no gaps/overlaps). Runs only on a storey **edit**, never on load/save, so it never rewrites an untouched project's saved elevations. Also added a `level.height > 0` check to `validateProject`.
  - `src/edit/commands.js` — five undoable, **lossless** storey commands: `addLevel` (appends on top, restacks), `removeLevel` (refuses to remove the last storey; captures the removed level + **all** prior elevations so undo is byte-identical), `renameLevel`, `setLevelHeight` (raising a lower storey pushes everything above it up; captures prior height + elevations), and `setLevelRoof` (used to keep the roof capping the **top** storey as the stack grows/shrinks). Every command captures enough to restore byte-identical state on undo (the Phase 1 guarantee).
  - `src/app/state.js` — `activeLevelId` + `setActiveLevel(id)`: the single "which storey am I editing" gate.
  - `src/app/planCanvas.js` — `setLevel(id)` retargets the plan surface at the active storey (the plan draws one storey at a time).
  - `src/main.js` — replaced the five hard-pinned `levels[0]` reads with `app.activeLevelId`; a **Pro-only** "Levels…" popover (list storeys top-to-bottom, click to edit, rename, per-storey floor-to-floor height, add/remove) behind the single `isAvailable('multi-level', mode)` seam gate; `reconcileActiveLevel()` in `refresh()` keeps the active storey valid across undo/redo (falls back to the ground floor if the edited storey is undone away); adding a storey moves the roof up so the stack always caps cleanly; walk-through now uses the active storey's eye height. Harness handles `__levels/__activeLevel/__addStorey/__removeStorey/__switchLevel/__setLevelHeight/__renameLevel/__levelSeamVisible`.
  - `index.html` — the "Levels…" toolbar group (hidden in Simple, shown in Pro) + the storey panel markup/styles, mirroring the existing snap-panel pattern.
- **Verified — engine-independent:** `node src/test/edit-core.test.mjs` → **215 passed, 0 failed** (was 189; **+26** in a new section 31 for `stackElevations`, all five commands do/undo/redo **byte-lossless**, elevation restacking (add on top / remove restacks survivors / height change pushes upper storeys), the **remove-last-storey guard**, roof transfer, the new height validation, and a `ToolController` integration proving a draw lands on the **active** storey not the ground floor). The **model/view-separation guard (30a–30i) stays green** — no engine leak. Regression: `phase2/test/phase2-core.test.mjs` still **33/33**.
- **Verified — headless (Three.js, Chromium/swiftshader, the Phase 0/1 method):** vendored three.js + three-bvh-csg + three-mesh-bvh via `git clone` (regenerated meshbvh's **12 `*.generated.js`** from `*.template.js` with a from-scratch nested `@if`/`@else`/`@endif` + `@echo` preprocess shim — npm/CDN blocked), importmap harness, drove the **real** `src/main.js` via raw CDP (Node's global `WebSocket`). Results — **zero console errors, zero page errors**. The seam is **hidden in Simple, revealed in Pro**; **add-storey** produced a 2nd storey stacked at elevation 2.7 and **moved the roof up** (ground `hasRoof:false`, upper `hasRoof:true`); drawing a 4-wall room upstairs landed **on the active upper storey** (ground stays 5 walls); the built scene then had **9 wall meshes** (5 ground + 4 upstairs), an **upper roof**, and `maxWallTop 5.4 m` (two stacked 2.7 m storeys) — `twoStoreys: true`; **setLevelHeight** on the ground floor (2.7→3.4) shifted the upper storey's elevation to 3.4. The **Phase 1 regression held throughout**: 5 walls / 4 CSG openings / 2 rooms / **1 GLB furniture** in the scene, **valid + lossless** after every storey edit. Screenshot: `docs/verification/phase3-multilevel.png` (a two-storey home in the 3D massing, ground-floor plan, "Levels…" seam armed in Pro).
- **Not touched (no regression):** Phase 1 model contract + lossless save (**no new save fields** — storeys use the pre-existing `Levels[]`; the active-storey id is view state, never serialized); the phase2 core + contract doc; CSG openings; GLB furniture loading; the scene builder (**unchanged** — it already rendered every level); model/view separation (all new logic is in pure `core/`/`app/`/`edit/` files, proven engine-free by the now-automated guard).
- **Next:** more Phase 3 depth behind the seam — **roof types beyond flat** (gable/hip; `roof.type` already exists in the model and `createRoof` carries it), and wiring the **import-a-plan / outsource-drawing** onboarding tiles (still "coming soon"). A per-storey furniture filter and a storey plan-thumbnail in the switcher are smaller follow-ups now that the active-storey plumbing exists.

## 2026-07-20 — Phase 2 close-out: automated model/view-separation guard + `PHASE-2-COMPLETE.md` sign-off
- **Picked:** the close-out that had been the flagged "Next" for three consecutive runs — every Phase 2 spine slice (S1–S6) and every Pro-seam tier-table row (exact dimensions, per-opening sizing, snapping/constraints) is landed, so the natural highest-value move is to *seal* Phase 2 with a consolidated regression + a sign-off doc like the Phase 0/1 docs. But a doc alone is thin, so I made the substance a **real reliability upgrade**: the model/view separation that Phase 0/1 declared *sacred* was, until today, re-checked *by hand* every run (by loading the pure modules under Node so they'd throw on the absent `three`). That misses a leak hidden behind an untaken branch or a lazy path. This run turns that hand-check into an automated test with teeth, then writes the sign-off on top of it. *(Branched from the true `origin/main` tip `d28a9ba` = PR #7; origin/main matched HEAD — no stale-ref trap this run.)*
- **Changed:**
  - `src/test/edit-core.test.mjs` — added an **automated model/view-separation guard** (assertions `30a`–`30i`): it statically scans every `.js`/`.mjs` under `src/` (excluding the test harness), flags any file that imports `three` / `three-bvh-csg` / `three-mesh-bvh` (static `from 'three'` **or** dynamic `import('three')`), and asserts the set of importers is **exactly** the seven-file render/build/pick allowlist (`build/*`, `viewer/viewer.js`, `viewer/walkCamera.js`, `edit/picking.js`). Fails on a **leak** into pure code *and* on a **stale allowlist** entry (a retired view file left dangling), so the invariant can't silently rot in either direction. Includes both-sides teeth-checks of the detector and a per-directory sweep proving `core/`, `app/`, `templates/`, the pure `edit/` and `viewer/` math stay engine-free.
  - `docs/PHASE-2-COMPLETE.md` (NEW) — the Phase 2 sign-off, in the shape of `PHASE-0-GONOGO.md`: definition-of-done table (all met, each mapped to its slice + headless screenshot), the Simple/Pro tier table (every row live), how it was verified (both suites + the per-slice headless proofs), the now-automated separation invariant, carried-forward caveats (CSG watertightness; **not** code-certified), and a Phase 3 recommendation.
- **Verified — engine-independent:** `node src/test/edit-core.test.mjs` → **189 passed, 0 failed** (was 173; +16 for the separation guard). Regression: `phase2/test/phase2-core.test.mjs` still **33/33**. **Proved the guard has teeth**, not just green-by-luck: injected `import * as THREE from 'three'` into `src/main.js` (a pure file the harness doesn't import at load, so the static scan — not the load-time crash — is what must catch it) → assertion `30b` failed naming `main.js` as the offender; reverted → back to 189/0 with only the test file changed. (Injecting into an *imported* pure module instead crashes the suite at load, which is the pre-existing load-time guard firing — both layers now cover the invariant.)
- **No headless render this run — and none required:** the change touches **zero** Three.js view-layer files (only the Node test harness + a Markdown doc), so the guardrail's headless-vendoring requirement (which governs three.js view-layer work) doesn't apply. The sign-off consolidates the per-slice headless proofs already captured and logged across the S1–S6 runs (screenshots in `docs/verification/`), plus this run's live green regression.
- **Not touched (no regression):** Phase 1 model contract + lossless save (no model fields added — this run adds a test + a doc only); the phase2 core + contract doc; CSG openings; GLB furniture loading; model/view separation (now *enforced* by the new guard rather than merely asserted). No `src/` product code changed.
- **Next:** Phase 2 is sealed. Phase 3 — depth behind the existing seam: **multi-level editing UI**, **roof types beyond flat**, and wiring the **import-a-plan / outsource-drawing** onboarding tiles (currently "coming soon"). Each is a self-contained slice verifiable the same two-tier way.

## 2026-07-18 — Pro seam, next row: configurable snapping / constraints (grid · corners · angle-lock)
- **Picked:** the last dormant Pro-seam tier-table row — **"Snapping/constraint settings" (Pro)**. `FEATURE_TIERS['snapping-constraints']` had been defined as `MODE.PRO` since S1 but was never wired to anything, exactly the shape yesterday's `exact-dimensions` was before it was surfaced. Worse, snapping was a *hardcoded fork*: Simple mode grid-snapped at a fixed 0.1 m and **Pro mode used the raw pointer with no snapping at all** — the opposite of what a precision user wants. This slice replaces that fork with three **data-driven** constraints (grid · vertex/corner · angle-lock), keeps Simple's previously-verified defaults byte-identical, and exposes the controls behind the seam so Pro users get real drawing precision. Corner snap also makes drawn rooms close **watertight**, which the CSG openings depend on. *(Branched from the true `origin/main` tip `ccffbf3` = PR #6; no stale-`origin/main` trap this run.)*
- **Changed:**
  - `src/edit/snapping.js` (NEW) — **pure, three-/DOM-free** snapping math: `defaultSnapSettings()` (grid+vertex on, angle off), `normalizeSnapSettings()` (clamps non-positive/out-of-range steps back to safe defaults so bad UI input can't divide-by-zero or loop), `nearestSnapVertex()` (snap onto an existing corner, with an `excludeKeys` set so a dragged corner never snaps to itself), `constrainAngle()` (lock a segment's direction to a multiple of `stepDeg` from its anchor, quantizing length to the grid when grid is on), and `snapPoint()` composing them in priority order **vertex → angle → grid** and returning which constraint fired. Node-testable; importing it under Node is itself the model/view-separation guard.
  - `src/app/state.js` — `createAppState` gains a `snap` settings tree + `setSnap(partial)` (deep-merges one field without restating the others). View-only pref — **never serialized**, so the Phase 1 save format is untouched.
  - `src/edit/tools.js` — `_snap(world, { anchor, excludeKeys })` now runs through `snapPoint` with the live settings instead of the old `mode === simple ? gridSnap : raw` fork. Wall-draw passes the previous corner as the angle anchor; corner-drag passes the grabbed endpoints as `excludeKeys`. Removed the dead `MODE_SIMPLE` gate.
  - `src/main.js` + `index.html` — a **Pro-only** "Snap…" popover (grid on/off + step, snap-to-corners on/off, angle-lock on/off + 15/30/45/90° selector) that edits `app.snap` live and redraws the plan. The `#snap-group` is shown only when `isAvailable('snapping-constraints', mode)` (the single seam gate), so it's invisible in Simple and appears in Pro; click-away closes it. Harness handles `__snap`/`__setSnap`/`__snapSeamVisible`.
- **Verified — engine-independent:** `node src/test/edit-core.test.mjs` → **173 passed, 0 failed** (was 153; +20 for defaults/normalization/clamping, `nearestSnapVertex` hit/miss/exclude, `constrainAngle` ortho + length-quantize + zero-length guard, `snapPoint` priority (vertex>angle>grid) + freeform, and a controller integration proving Pro angle-lock snaps an off-axis draw to ortho + `setSnap` deep-merge). Regression: `phase2/test/phase2-core.test.mjs` still **33/33**.
- **Verified — headless (Three.js, Chromium/swiftshader, the Phase 0/1 method):** rebuilt the vendor + harness from scratch (npm/CDN blocked): `git clone` three.js + three-bvh-csg + three-mesh-bvh, regenerated meshbvh's **12 `*.generated.js`** from `*.template.js` with a from-scratch nested `@if`/`@else`/`@endif` + `@echo INDIRECT_STRING` preprocess shim, importmap harness, drove the **real** app via raw CDP (Node's global `WebSocket`, no playwright pkg). Results — **14/14 checks pass, zero console/page errors**; Phase 1 regression intact (5 walls / 4 CSG openings / lossless, GLB furniture in the scene). The seam is **hidden in Simple, revealed in Pro**; the panel opens in Pro; **angle-lock** snapped an off-axis draw (0,0)→(4,0.4) to a clean ortho wall (`b.z = 0`); **corner snap** landed a click 0.06 m off an existing corner *exactly* on it (and away from the raw click); **freeform** (all off) used the raw click point verbatim; the model stayed valid + lossless after every snapped edit. Screenshots: `docs/verification/phase2-snap-panel.png` (Pro snapping panel — grid 0.1 m + corners on, angle-lock off with its selector disabled) and `phase2-snap-drawn.png` (angle-locked + vertex-snapped L of walls in plan, mirrored in the 3D massing, HUD "lossless · mode: pro").
- **Not touched (no regression):** Phase 1 model contract + lossless save (snapping adds **no model fields** — settings live in view state only); the phase2 core + contract doc; CSG openings; GLB furniture loading; model/view separation (`core/`, `templates/`, `app/*` except none-three, `edit/snapping.js`, `edit/tools.js`, `edit/planView.js`, `viewer/cutaway.js`, `viewer/walk.js` all three-free; three only in `viewer/viewer.js`, `viewer/walkCamera.js`, `build/`, `edit/picking.js`).
- **Next:** every Pro-seam tier-table row (exact dimensions, per-opening sizing, snapping/constraints) is now landed — the natural close-out is a consolidated **regression + `PHASE-2-COMPLETE.md` sign-off** (like the Phase 0/1 docs). Then Phase 3 depth behind the existing seam: multi-level editing, roof types beyond flat, and the import-a-plan / outsource onboarding tiles (currently shown "coming soon").

## 2026-07-17 — Pro seam, activated: selection inspector + exact-dimension entry
- **Picked:** With the Phase 2 spine (S1–S6) all landed, the highest-value gap was the **dormant Simple/Pro seam** — `FEATURE_TIERS` had `exact-dimensions: PRO` defined since S1 but it was never surfaced, and *selecting an object showed nothing* (the S1 "minimal inspector" acceptance was never actually built into the UI). This slice delivers the Phase 2 tier-table row *"Exact dimension entry (length/thickness/sill)"*: a selection inspector that reads out measurements in Simple mode and becomes exact-entry inputs (parsed via `units.js`) in Pro — the first time the seam is visible to a user. *(Branched from the true S4 tip `25852a9`; no stale-`origin/main` trap this run.)*
- **Changed:**
  - `src/app/inspector.js` (NEW) — **pure, three-/DOM-free** selection descriptor: `describeSelection(project, selection, {mode, units})` returns the selected wall's (length/thickness/height) or opening's (width/height/sill/offset) dimensions, formatted per display units, with an `editable` flag driven by `isAvailable('exact-dimensions', mode)` — so the Simple/Pro gate lives in *one* place, not scattered ifs. `buildDimensionEdit(...)` parses raw input (`10'6"`, `3.2m`, `320cm`), range-guards positive vs. non-negative fields, and returns an undoable command. Node-testable; importing it under Node is itself the separation guard.
  - `src/edit/commands.js` — added `resizeWall` (length moves endpoint b along the a→b axis keeping a fixed; thickness/height set) and `resizeOpening` (width/height/sill/offset). Both capture the **full prior geometry** and restore it on undo, so a resize→undo round-trip stays byte-identical (the Phase 1 lossless guarantee).
  - `src/main.js` — the inspector panel + commit flow: a committed edit runs through `history`, is **re-validated** (`validateProject`), and on failure the command is rolled back *and popped from history* so an out-of-range value (e.g. an opening wider than its wall, or shrinking a wall below its door's offset) never corrupts the model or the save round-trip. Added the **Simple/Pro** toggle (the single UI gate) and a **m ↔ ft-in** display-units toggle (storage stays metric). Wired inspector refresh into plan-select, 3D-raycast-select, and every `refresh()`.
  - `src/app/planCanvas.js` — added an `onSelect` hook so plan-view selections refresh the inspector (view-only; still no Three.js, no stored geometry).
  - `index.html` — inspector panel markup + styles; Simple/Pro + units button groups in the toolbar.
- **Verified — engine-independent:** `node src/test/edit-core.test.mjs` → **153 passed, 0 failed** (was 130; +23 for `resizeWall`/`resizeOpening` do/undo lossless + error paths, `describeSelection` Simple-readonly vs. Pro-editable + imperial formatting + null/room selection, and `buildDimensionEdit` parse/range-guard/apply-lossless/over-range-rejection). Regression: `phase2/test/phase2-core.test.mjs` still **33/33**.
- **Verified — headless (Three.js, Chromium/swiftshader, the Phase 0/1 method):** rebuilt the vendor + harness from scratch (npm/CDN blocked): `git clone` three.js r169 + three-bvh-csg + three-mesh-bvh, regenerated meshbvh's **12 `*.generated.js`** from its `*.template.js` with a from-scratch `preprocess` shim (handles nested `@if INDIRECT`/`@else`/`@endif` + `@echo INDIRECT_STRING`), importmap harness, drove the **real** app via raw CDP (Node's global `WebSocket`). Results — **zero console/page errors**; Phase 1 regression intact (5 walls / 4 CSG openings / lossless, GLB furniture in the scene). Selecting a wall shows the inspector; **Simple = 0 input fields (read-only), Pro = 3 input fields**; committing thickness **0.12→0.25 m** kept the project valid + lossless; an imperial length entry (`30'0"`) grew the wall **8→9.144 m** by moving its endpoint (and Simple readout formats `8.00 m` as `26′ 3″`); an over-wide opening (`99 m`) and an under-length wall shrink were both **rejected and rolled back** (model unchanged, still valid + lossless); undoing the two accepted edits returned to a valid, lossless baseline. Screenshot: `docs/verification/phase2-pro-inspector.png` (Pro toggle active, Wall inspector with Length/Thickness/Height fields, selected wall highlighted in plan, 3D cutaway with furniture).
- **Not touched (no regression):** Phase 1 model contract + lossless save (the inspector adds **no model fields** — resize commands only edit existing `walls[]`/`openings[]` values); the phase2 core + contract doc; CSG openings; GLB furniture loading; model/view separation (`core/`, `templates/`, `app/dirty.js`, `app/thumbnail.js`, `app/inspector.js`, `app/state.js`, `edit/` (except `picking.js`), `viewer/cutaway.js`, `viewer/walk.js` all three-free; three only in `viewer/viewer.js`, `viewer/walkCamera.js`, `build/`, `edit/picking.js`).
- **Next:** the remaining Pro-seam tier-table rows — **per-opening custom sizing UI** is now covered by the same inspector; next up are **snapping/constraint settings** (Pro) and the Phase 2 close-out: a consolidated regression + `PHASE-2-COMPLETE.md` sign-off (like the Phase 0/1 docs). Then Phase 3 depth (multi-level editing, roof types, import-a-plan onboarding) behind the existing seam.

## 2026-07-16 — S4: start-screen template picker in the runnable app
- **Picked:** the last remaining Phase 2 spine slice per `docs/PHASE-2-PLAN.md` and the S6 "Next" note — **S4 start-screen template picker** (blank / starter templates, confirm-on-unsaved, reframe on load). S1–S3, S5, S6 were landed; the picker is the last "definition of done" onboarding item and it replaces the ad-hoc live starter dropdown with a real start screen, completing the end-to-end skeleton. *(Also caught the same stale-`origin/main` trap the S6 entry flagged: my fresh clone's `origin/main` pointed at the Phase 0/1 baseline `41bb218`; re-fetched and re-based the branch onto the true S6 tip `77b7c6e` before building.)*
- **Changed:**
  - `src/app/dirty.js` (NEW) — **pure, engine-free** unsaved-changes tracker: "dirty" is just the current serialized project vs. the last clean baseline, so it rides the Phase 1 lossless round-trip (a real edit flips it dirty, undo-all flips it clean) with no flags scattered through the edit layer. Node-testable; importing it under Node is itself the separation guard.
  - `src/app/thumbnail.js` (NEW) — **pure, three-/DOM-free** top-down SVG plan thumbnail from a project's wall centerlines (fitted, aspect-preserving viewBox; blank → an empty framed grid). Novices pick a *shape*, not a name. Returns an SVG string.
  - `src/templates/starters.js` — added a one-line `desc` to each starter for the picker cards (no model change).
  - `src/main.js` — the picker: renders a card per starter (live SVG thumbnail + description) plus disabled Import/Outsource tiles (RoomSketcher's blank/template/import/outsource pattern; import+outsource are Phase 3+). `loadStarter` swaps via the existing undoable `loadTemplate` command, reframes both views, and rebases the clean baseline. Opens as the start screen on boot; a toolbar **Templates…** button reopens it, confirming first if there are unsaved changes (`confirmDiscard` is injectable so the headless harness drives it deterministically). Esc closes the picker. Replaced the old live `<select>` starter dropdown.
  - `index.html` — picker overlay markup + styles; **Templates…** button replaces the `Start:` dropdown.
- **Verified — engine-independent:** `node src/test/edit-core.test.mjs` → **130 passed, 0 failed** (was 114; +16 for the dirty tracker do/edit/undo/rebase and the thumbnail bounds/one-line-per-wall/blank-grid/every-starter-renders assertions). Regression: `phase2/test/phase2-core.test.mjs` still **33/33**.
- **Verified — headless (Three.js, Chromium/swiftshader, the Phase 0/1 method):** vendored three.js (r169) + three-bvh-csg + three-mesh-bvh (regenerated its 12 `*.generated.js` from `*.template.js` with a minimal `@echo`/`@if`/`@else`/`@endif` preprocess shim — npm/CDN blocked), importmap harness, drove the **real** app via raw CDP (Node's global `WebSocket`, no playwright pkg). Results — **zero console/page errors**; Phase 1 regression intact (5 walls / 4 CSG openings / lossless round-trip, GLB furniture in the scene). Picker **opens as the start screen on boot** with 5 cards (3 enabled starters + 2 disabled "coming soon"), thumbnails render from wall data; picking **Studio** loads it (4 walls / 2 openings), closes the picker, reframes, validates + round-trips lossless and reads **clean**; an edit flips it **dirty**, reopening shows the *"unsaved changes"* note; **declining** the confirm keeps Studio (still dirty), **accepting** swaps in the two-room home (5/4, clean again, lossless). Screenshots: `docs/verification/phase2-s4-picker.png` (start screen with plan thumbnails over the massing), `phase2-s4-studio.png` (studio loaded — plan + reframed 3D), `phase2-s4-loaded.png`.
- **Not touched (no regression):** Phase 1 model contract + lossless save (the picker adds no model fields — `desc` is picker-only metadata, not serialized); the phase2 core + contract doc; CSG openings; GLB furniture loading; model/view separation (`core/`,`templates/`,`app/dirty.js`,`app/thumbnail.js`,`app/state.js`,`edit/`(except `picking.js`),`viewer/cutaway.js`,`viewer/walk.js` all three-free; three only in `viewer/viewer.js`,`viewer/walkCamera.js`,`build/`,`edit/picking.js`).
- **Next:** Phase 2's full spine (S1–S6) is now landed — template picker → draw walls → cut openings → exterior/cutaway → walk-through, every edit undoable + lossless. Remaining to close Phase 2: a consolidated **regression + visual-proof pass** and a `PHASE-2-COMPLETE.md` sign-off (like the Phase 0/1 docs). Then Phase 3 / Pro-seam depth (exact-dimension entry, per-opening sizing, import-a-plan onboarding) behind the existing seam.

## 2026-07-15 — S6: first-person walk-through camera in the runnable app
- **Picked:** the last Phase 2 spine slice per `docs/PHASE-2-PLAN.md` and the S5 "Next" note — **S6 walk-through camera** (WASD/arrows move, drag to look, toggled against orbit, Esc → orbit). With walls/openings editable (S2/S3) and the exterior↔cutaway view (S5) landed, walking the space at eye level is the remaining "definition of done" item and completes the end-to-end skeleton. Pure view state — no model fields — so it never touches the Phase 1 save format. *(Also caught and corrected a stale local `origin/main` ref that briefly reset the tree to the Phase 0/1 baseline; re-based the branch onto the true S5 tip 1666090 before building.)*
- **Changed:**
  - `src/viewer/walk.js` (NEW) — **pure, three-free** walk math: `forwardXZ`/`rightXZ`/`lookDir` heading basis, `eyeHeight` (level elevation + 1.6 m), `levelBoundsXZ`(inset footprint, collapses an over-inset axis to its midpoint) + `clampToBounds`, `stepWalk` (normalized diagonal movement, pitch never affects ground travel), `stepLook` (yaw/pitch update with pitch clamped just shy of vertical). Node-testable; importing it under Node (no `three`) is itself the separation guard.
  - `src/viewer/walkCamera.js` (NEW) — the **three-side** controller: drives the *existing* perspective camera at eye height from the pure math each frame via the viewer's `onFrame` hook, WASD/arrow keydown/up + drag-look pointer handlers, footprint clamping. Suspends orbit input while walking and restores it on exit. `integrate(dt)` is extracted so the headless harness can step deterministically. The **only** new `three` importer this slice.
  - `src/main.js` — Orbit/Walk toolbar toggle (`setCamera`), Esc exits walk, walk-mode guards (drag = look, not select-raycast; template-load leaves walk cleanly so bounds never go stale), camera mode shown in the HUD, harness handles (`__walk`, `__setCamera`).
  - `index.html` — Orbit/Walk button group.
- **Verified — engine-independent:** `node src/test/edit-core.test.mjs` → **114 passed, 0 failed** (was 89; +25 for the walk math: heading basis ⟂ + unit vectors, lookDir↔forward agreement + pitch tilt, eye height, bounds inset/collapse/clamp, forward/strafe/diagonal-normalized/opposing-cancel movement, look turn + pitch clamp). Regression: `phase2/test/phase2-core.test.mjs` still **33/33**.
- **Verified — headless (Three.js, Chromium/swiftshader, the Phase 0/1 method):** vendored three.js + three-bvh-csg + three-mesh-bvh (regenerated its 12 `*.generated.js` from `*.template.js` with a minimal `preprocess` shim, npm/CDN blocked), importmap harness, drove the **real** app via raw CDP (Node's global `WebSocket`; no playwright pkg). Results — **zero console/page errors**; Phase 1 regression intact (5 walls / 4 CSG openings / lossless round-trip, furniture GLB loads); Orbit→Walk drops the camera to **eye height 1.60 m**, holding W moved the walker 2 m and strafe 1 m, it **stayed inside the footprint** bounds, a **real dispatched-PointerEvent drag turned the view** (yaw 0 → 0.83), and Esc/Orbit **restored orbit control**; model still validates + round-trips lossless after walking. Screenshots: `docs/verification/phase2-s6-walk.png` (interior at eye level — ceiling/floor depth) + `phase2-s6-orbit.png` (full exterior massing, orbit restored).
- **Not touched (no regression):** Phase 1 model contract + lossless save (walk adds no model fields); the phase2 core + contract doc; CSG openings; GLB furniture loading; model/view separation (`core/`,`templates/`,`app/`,`edit/`(except `picking.js`),`viewer/cutaway.js`,`viewer/walk.js` all three-free; three only in `viewer/viewer.js`,`viewer/walkCamera.js`,`build/`,`edit/picking.js`).
- **Next:** Phase 2's S1–S3, S5, S6 are landed — the end-to-end skeleton (template → draw walls → cut openings → exterior/cutaway → walk-through, all undoable + lossless) is complete. Remaining: **S4** start-screen template picker (confirm-on-unsaved + reframe) to finish the onboarding surface, then a Phase 2 regression + `PHASE-2-COMPLETE.md` sign-off. Pro-mode exact-dimension entry still sits behind the existing seam.

## 2026-07-14 — S5: exterior ↔ cutaway view toggle in the runnable app
- **Picked:** the next spine slice per `docs/PHASE-2-PLAN.md` — S5 exterior/cutaway. This delivers a headline "definition of done" item (*"toggle between exterior and interior/cutaway view"*) and is the natural next step after S1–S3: with walls + openings editable, you now need to see *inside* the massing. S4 (start-screen picker) is deferred — a live starter dropdown already covers template swapping, so the interior view is the higher-value gap.
- **Changed:**
  - `src/edit/commands.js` — added `setView(view)`: an **undoable, persisted display pref** written to `project.meta.view`. `do()`/`undo()` touch only `meta`, so scene geometry (and object count) is never mutated and the lossless save round-trip is preserved. `validateProject` ignores `meta`, so a view pref round-trips byte-identically.
  - `src/viewer/cutaway.js` (NEW) — **pure, three-free** cutaway decision: `wallsCenterXZ` + `cutawayHiddenWalls(walls, cameraXZ)` return the ids of the camera-side walls that occlude the interior (radial dot-product test against the footprint centroid). Node-testable; importing it under Node (no `three`) is itself the separation guard.
  - `src/viewer/viewer.js` — added a model-agnostic per-frame hook (`onFrame`) run just before each render, so cutaway visibility **self-corrects as the camera orbits**.
  - `src/main.js` — `applyView()` per-frame updater (hides roof + camera-side walls in cutaway; floors/furniture/ground stay visible); Exterior/Cutaway toolbar toggle wired through `history.execute(setView(v))`; view seam re-synced on every refresh so undo/redo/template-load keep the buttons honest.
  - `index.html` — Exterior/Cutaway button group.
- **Verified — engine-independent:** `node src/test/edit-core.test.mjs` → **89 passed, 0 failed** (was 75; +14 for `setView` do/undo/persist/lossless + `cutawayHiddenWalls` camera-side/far-side/degenerate-guard math). Regression: `phase2/test/phase2-core.test.mjs` still **33/33**.
- **Verified — headless (Three.js, Chromium/swiftshader, the Phase 0/1 method):** vendored three.js + three-bvh-csg + three-mesh-bvh (regenerated its `*.generated.js` from `*.template.js` via a minimal `preprocess` shim, npm blocked), importmap harness, drove the real Exterior/Cutaway toggle. Results — **zero console/page errors**; Phase 1 regression intact (5 walls / 4 CSG openings / lossless); exterior = roof + all 5 walls visible; cutaway = roof hidden + 2 near walls hidden + 3 far walls framing the interior, **object count stable** (visibility-only) and model still validates + round-trips lossless; `setView` undo returns to exterior. Screenshots: `docs/verification/phase2-s5-exterior.png` (closed massing) + `phase2-s5-cutaway.png` (interior + furniture revealed).
- **Not touched (no regression):** Phase 1 model contract + lossless save; the phase2 core + contract doc; CSG openings; GLB furniture loading (still loads in the harness); model/view separation (`core/`,`templates/`,`app/`,`edit/`,`viewer/cutaway.js` all three-free; three only in `viewer/viewer.js`,`build/`,`edit/picking.js`).
- **Next:** **S6** walk-through camera (WASD + pointer-look at eye height, toggled against orbit, Esc → orbit) — pure view state, verified the same headless way. Then optionally **S4** start-screen picker (confirm-on-unsaved) to complete the onboarding surface. Pro-mode exact-dimension entry still sits behind the existing seam.

## 2026-07-13 — S2 + S3 view layer: draw/edit walls and cut openings in the runnable app
- **Picked:** the next spine slice per `docs/PHASE-2-PLAN.md` — wire the S1 command/history core into a real interactive **plan-view** surface so a user draws walls and places a door/window by clicking, each action driving the plain-model commands and re-deriving the Three.js scene. This is what turns Phase 1's static model into an editable app. (First reconciled the branch onto the freshly-merged `origin/main` = PR #1 / S1, which my local clone had stale.)
- **Changed:**
  - `src/edit/planView.js` (NEW) — pure screen↔world mapping (top-down ortho over XZ) + pure plan-space hit-testing (`pointToSegment`, `nearestVertex`, `nearestWallHit`, `verticesAt`, `snap`). **Zero Three.js** → fully node-testable.
  - `src/edit/tools.js` (NEW) — `ToolController` state machine (select / draw-wall / place-door / place-window). Gestures build S1 commands, commit via `History`, then ask the view to rebuild. Corner-drag coalesces into **one** `composite` `moveWallVertex` moving every wall sharing the corner; wall-draw chains corners; opening placement clamps to fit. **Zero Three.js.**
  - `src/edit/commands.js` — added `composite(name, cmds)`; **fixed a lossless-undo bug** in `removeWall` (openings now restored at their original indices, so remove→undo stays byte-identical even when the wall's opening wasn't last).
  - `src/edit/picking.js` (NEW) — `raycast()` (THREE.Raycaster → `userData.modelId/kind`) for 3D-view selection. The **only** three importer in `edit/`.
  - `src/build/sceneBuilder.js` — split out `buildGeometry()` (sync, pure) and added `rebuildGeometry(group, project)` — the incremental rebuild that re-derives walls/openings/floors/roof after each edit while leaving loaded furniture GLBs in place.
  - `src/app/planCanvas.js` (NEW) — the 2D plan renderer + pointer wiring (Canvas 2D, no three): grid, walls, carved-opening markers, corner dots, rubber-band preview.
  - `src/main.js` + `index.html` — toolbar (Select / Draw wall / Door / Window / Delete / Undo / Redo), plan|3D split, starter dropdown (drives the existing `loadTemplate`), keyboard (Esc/Del/Ctrl+Z/Y), 3D raycast-select. Every edit rebuilds 3D + redraws plan + re-runs the lossless self-test.
- **Verified — engine-independent:** `node src/test/edit-core.test.mjs` → **75 passed, 0 failed** (was 46; +29 for planView math, hit-testing, controller draw-a-room, corner-drag-moves-both-walls, place door+window, delete-cascade+undo, composite). Importing `planView.js`/`tools.js` under Node (which has no `three`) is itself the separation guard. Regression: `phase2/test/phase2-core.test.mjs` still **33/33**.
- **Verified — headless (Three.js, Chromium/swiftshader via CDP, the Phase 0/1 method):** vendored three.js + three-bvh-csg + three-mesh-bvh (regenerated its `*.generated.js` from `*.template.js` with a minimal `preprocess` shim, npm being blocked), importmap harness, drove the tool controller with synthetic gestures. Results: boots with **zero console errors**; Phase 1 regression intact (sample = 5 walls / 4 CSG openings / lossless); drew a 4-wall room (wall tris 48 = 4 clean boxes) → placed door+window (**tris 48→147, CSG carved**) → undo/redo openings → edited project still validates + round-trips lossless. Screenshot: `docs/verification/phase2-s2s3.png` (plan room + 3D massing with carved door/window).
- **Not touched (no regression):** Phase 1 model contract + lossless save; the phase2 core + contract doc; GLB furniture loading (still loads in the harness); model/view separation (`core/`,`templates/`,`app/` three-free; three only in `viewer/`,`build/`,`edit/picking.js`).
- **Next:** **S4** template picker start-screen (confirm-on-unsaved + reframe) and **S5** exterior↔cutaway view toggle (`SetView` display pref), then **S6** walk-through camera. Pro-mode exact-dimension entry for wall length/thickness/opening size sits behind the existing seam.

## 2026-07-12 — S1 interaction foundation landed in the runnable `src/` app
- **Picked:** Integrate the engine-independent Phase 2 S1 core (commands + history + starters) into the runnable `src/` tree, reconciled onto the *authoritative* `src/core/model.js`. This is the foundation S2–S6 build on, and the one slice fully verifiable in this sandbox (no Three.js needed). The `phase2/` copy targeted its own divergent model API (`makeWall`/`findLevel`/center-offset openings); none of it was wired into `src/`.
- **Changed:**
  - `src/core/model.js` — added pure lookups `findLevel` / `findWall` / `findOpening` (additive; no change to existing factories, validation, or the save format).
  - `src/edit/commands.js` (NEW) — `addWall`, `moveWallVertex`, `removeWall` (cascades to its openings), `addOpening`, `removeOpening`, `loadTemplate`. Each is a `do()`/`undo()` on the plain `Project`. Zero Three.js.
  - `src/edit/history.js` (NEW) — model-level undo/redo stack with a size limit.
  - `src/templates/starters.js` (NEW) — `blank` / `studio` / two-room (`sampleHome`) as pure data, using src's `createWall`/`createOpening` **near-edge offset** convention (reconciled from phase2's center-offset).
  - `src/app/state.js` — `createAppState` gains `activeTool` / `view` / `camera` + setters and `TOOL`/`VIEW`/`CAMERA` enums (additive; existing `FEATURE_TIERS`/`isAvailable`/`availableTools` untouched).
  - `package.json` — `npm run test:edit`.
- **Verified (engine-independent, per the run rules):** `node src/test/edit-core.test.mjs` → **ALL PASS — 46 passed, 0 failed**. Covers: sample-home validate + counts; **lossless serialize round-trip survives every edit** (byte-identical); draw-a-room via commands; vertex move + undo/redo; opening near-edge fit detection; `removeWall`/`removeOpening` cascade + lossless undo; full undo-all byte-identical; redo-stack clear on new command; history limit trimming; `loadTemplate` in-place swap + undo; all starters validate; command error paths throw on bad ids. Regression guard: `phase2/test/phase2-core.test.mjs` still **33/33**. Model/view separation confirmed — no `three` import in `core/`, `edit/`, `templates/`, `app/`.
- **Not touched (no regression):** render path (`sceneBuilder`, `viewer`, `main.js`, `furniture`, CSG) is unchanged; the new edit layer is not yet wired into `main.js`.
- **Next:** S2/S3 **view layer** — wire `src/edit/` into `main.js` behind a plan-view interaction surface (`picking.js`, `tools.js`, `planView.js`) so drawing walls / placing openings drives these commands and rebuilds the scene. Requires the Three.js headless harness (clone three.js + three-bvh-csg + three-mesh-bvh, Playwright/swiftshader) to verify per the Phase 0/1 method.

## 2026-07-10 — setup — repo assembled for autonomous daily dev
- Did: assembled the push-ready repo (Phase 1 runnable app + Account A's Phase 2 core, 33/33 core tests passing) and authored the autonomous daily-run prompt (`automation/daily-dev-run.md`) + setup guide.
- State: ready to push to GitHub and schedule at 5am ET (see `automation/SETUP.md`).
- Next: first autonomous run advances Phase 2 (next unbuilt slice per `docs/PHASE-2-PLAN.md`).
